
Solicitação de arquivos para Sprint 2
PS C:\Users\pc\Documents\juridicwatcher> make test-unit
go test -count=1 ./internal/...
?       github.com/F3rreir4L19/juridico-watcher/internal/domain [no test files]
# github.com/F3rreir4L19/juridico-watcher/internal/service_test [github.com/F3rreir4L19/juridico-watcher/internal/service.test]
internal\service\monitor_service_test.go:4:2: "os" imported and not used
ok      github.com/F3rreir4L19/juridico-watcher/internal/engine 0.875s
ok      github.com/F3rreir4L19/juridico-watcher/internal/pdf    0.830s
FAIL    github.com/F3rreir4L19/juridico-watcher/internal/service [build failed]
ok      github.com/F3rreir4L19/juridico-watcher/internal/storage        1.143s
ok      github.com/F3rreir4L19/juridico-watcher/internal/watcher        1.141s
FAIL
make: *** [test-unit] Error 1
PS C:\Users\pc\Documents\juridicwatcher>
O usuário está executando os testes unitários após a implementação do Sprint 6 (camada de serviços). O erro agora está no arquivo de teste internal/service/monitor_service_test.go, que está importando "os" sem usar. Preciso corrigir isso, removendo o import não utilizado.

Vou fornecer a correção do arquivo de teste, garantindo que os imports estejam limpos e o teste funcione.

O erro está apenas no arquivo de teste: monitor_service_test.go importa "os" sem usar. Remova essa linha.

Correção nos imports de internal/service/monitor_service_test.go:

go
import (
	"path/filepath"
	"testing"
	"time"

	"github.com/F3rreir4L19/juridico-watcher/internal/domain"
	"github.com/F3rreir4L19/juridico-watcher/internal/service"
	"github.com/F3rreir4L19/juridico-watcher/internal/storage"
	"github.com/F3rreir4L19/juridico-watcher/test/testhelpers"
	"github.com/stretchr/testify/assert"
	"github.com/stretchr/testify/require"
)
O restante do arquivo permanece igual. Execute make test-unit novamente.

PS C:\Users\pc\Documents\juridicwatcher> make test-unit
go test -count=1 ./internal/...
?       github.com/F3rreir4L19/juridico-watcher/internal/domain [no test files]
ok      github.com/F3rreir4L19/juridico-watcher/internal/engine 0.981s
ok      github.com/F3rreir4L19/juridico-watcher/internal/pdf    0.773s
--- FAIL: TestMonitorService_EndToEnd (1.03s)
    monitor_service_test.go:58: 
                Error Trace:    C:/Users/pc/Documents/juridicwatcher/internal/service/monitor_service_test.go:58
                Error:          file "C:\\Users\\pc\\AppData\\Local\\Temp\\TestMonitorService_EndToEnd2804881219\\002\\doc.pdf" exists
                Test:           TestMonitorService_EndToEnd
    monitor_service_test.go:59: 
                Error Trace:    C:/Users/pc/Documents/juridicwatcher/internal/service/monitor_service_test.go:59
                Error:          unable to find file "C:\\Users\\pc\\AppData\\Local\\Temp\\TestMonitorService_EndToEnd2804881219\\002\\Joao\\doc.pdf"
                Test:           TestMonitorService_EndToEnd
    monitor_service_test.go:64: 
                Error Trace:    C:/Users/pc/Documents/juridicwatcher/internal/service/monitor_service_test.go:64
                Error:          "[]" should have 1 item(s), but has 0
                Test:           TestMonitorService_EndToEnd
FAIL
FAIL    github.com/F3rreir4L19/juridico-watcher/internal/service        1.757s
ok      github.com/F3rreir4L19/juridico-watcher/internal/storage        1.184s
ok      github.com/F3rreir4L19/juridico-watcher/internal/watcher        1.210s
FAIL
make: *** [test-unit] Error 1
PS C:\Users\pc\Documents\juridicwatcher>
O teste de integração falhou porque o arquivo não foi processado dentro do tempo de espera. Precisamos investigar se o monitoramento está capturando o evento e se o pipeline está sendo executado. Vamos ajustar o teste para ser mais robusto:

O TestMonitorService_EndToEnd escreve um PDF depois que o monitoramento já foi iniciado. O watcher fsnotify gera um evento Create imediatamente, mas no Windows pode haver um pequeno atraso.

O estabilizador (WaitStable) espera 2 ciclos de 500ms (total 1s). Com o Sleep de 1 segundo no teste, pode não ser suficiente para todo o fluxo (evento + goroutine + estabilização + pipeline + mover arquivo). Aumentamos o Sleep para 2 segundos, mas ainda falhou.

O erro diz que o arquivo original ainda existe e a pasta Joao não foi criada, indicando que o pipeline não executou a ação.

Possíveis causas:

O watcher não detectou o arquivo porque ele foi criado antes de adicionar o diretório ao watcher? Mas o teste cria o PDF após StartMonitoring(), então deve ser detectado.

O caminho do watch registrado é monDir (absoluto). O watcher usa filepath.Abs internamente. Deve funcionar.

O StartMonitoring itera sobre todos os watches ativos e inicia um watcher para cada. Como só há um, deve iniciar.

Pode ser que o evento Create não esteja sendo emitido porque o arquivo foi escrito de forma síncrona e o fsnotify no Windows precisa que o arquivo seja fechado? WritePDF fecha o arquivo. O gofpdf chama OutputFileAndClose, então o arquivo é fechado.

Vamos modificar o teste para usar um polling explícito em vez de um Sleep fixo, verificando periodicamente se o arquivo foi movido, com timeout. Isso é mais confiável.

Além disso, adicionaremos um log para debugging, mas como é teste, podemos apenas aumentar o timeout.

Vou fornecer uma nova versão do teste com polling.

Solução: substituir o time.Sleep por um loop que verifica a existência do arquivo esperado, com timeout de 5 segundos.

go
func TestMonitorService_EndToEnd(t *testing.T) {
    // ... setup igual ...
    mon := service.NewMonitorService(db, nil)
    err := mon.StartMonitoring()
    require.NoError(t, err)
    defer mon.StopAll()

    pdfPath := testhelpers.WritePDF(t, monDir, "doc.pdf", "Nome: Joao\nConteudo")

    // Espera ativa: o arquivo deve ser movido para subpasta "Joao"
    expectedPath := filepath.Join(monDir, "Joao", "doc.pdf")
    require.Eventually(t, func() bool {
        _, err := os.Stat(expectedPath)
        return err == nil
    }, 5*time.Second, 200*time.Millisecond, "arquivo não foi movido para %s", expectedPath)

    // Verifica que o original não existe mais
    assert.NoFileExists(t, pdfPath)

    list, err := pr.ListRecent(1)
    require.NoError(t, err)
    require.Len(t, list, 1)
    assert.Equal(t, domain.StatusSuccess, list[0].Status)
}
Precisamos importar "os" novamente para usar os.Stat. Então o import será:

go
import (
    "os"
    "path/filepath"
    "testing"
    "time"
    ...
)
Mas o arquivo atualmente não importa "os". Vamos ajustar.

Também devemos garantir que o processedRecorder está registrando. O pipeline usa recorder.Record, que chama pr.Record. O ListRecent deve retornar o registro.

Essa alteração deve resolver o teste. Peço que substitua o conteúdo do monitor_service_test.go pela versão corrigida abaixo.

O teste falhou porque o tempo fixo de espera não foi suficiente. Vamos usar um polling com Eventually, muito mais robusto.

Substitua todo o conteúdo de internal/service/monitor_service_test.go por:

go
package service_test

import (
	"os"
	"path/filepath"
	"testing"
	"time"

	"github.com/F3rreir4L19/juridico-watcher/internal/domain"
	"github.com/F3rreir4L19/juridico-watcher/internal/service"
	"github.com/F3rreir4L19/juridico-watcher/internal/storage"
	"github.com/F3rreir4L19/juridico-watcher/test/testhelpers"
	"github.com/stretchr/testify/assert"
	"github.com/stretchr/testify/require"
)

func TestMonitorService_EndToEnd(t *testing.T) {
	db := testhelpers.TempDB(t)
	monDir := t.TempDir()

	wr := storage.NewWatchRepo(db)
	rr := storage.NewRuleRepo(db)
	pr := storage.NewProcessedRepo(db)

	w := &domain.Watch{Name: "pasta", Path: monDir, Active: true, Recursive: false}
	require.NoError(t, wr.Create(w))

	rule := &domain.Rule{
		Name:     "Organiza",
		Priority: 1,
		Active:   true,
		WatchIDs: []int64{w.ID},
		Extractions: []domain.Extraction{
			{VariableName: "nome", StartDelim: "Nome: ", EndDelim: "\n"},
		},
		Actions: []domain.Action{
			{Type: domain.ActionCreateFolder, Target: "{nome}", Order: 1},
			{Type: domain.ActionMove, Target: "{nome}", Order: 2},
		},
	}
	require.NoError(t, rr.Create(rule))

	mon := service.NewMonitorService(db, nil)
	err := mon.StartMonitoring()
	require.NoError(t, err)
	defer mon.StopAll()

	// Cria o PDF depois que o monitoramento já iniciou
	pdfPath := testhelpers.WritePDF(t, monDir, "doc.pdf", "Nome: Joao\nConteudo")

	// Espera ativa: arquivo deve aparecer na subpasta Joao
	expectedPath := filepath.Join(monDir, "Joao", "doc.pdf")
	require.Eventually(t, func() bool {
		_, err := os.Stat(expectedPath)
		return err == nil
	}, 5*time.Second, 200*time.Millisecond, "arquivo não foi movido para %s", expectedPath)

	// Arquivo original não existe mais
	assert.NoFileExists(t, pdfPath)

	// Registro no banco
	list, err := pr.ListRecent(1)
	require.NoError(t, err)
	require.Len(t, list, 1)
	assert.Equal(t, domain.StatusSuccess, list[0].Status)
}
Execute make test-unit. O teste agora espera até 5 segundos, verificando a cada 200ms, até o arquivo aparecer no destino. Deve passar.

PS C:\Users\pc\Documents\juridicwatcher> make test-unit
go test -count=1 ./internal/...
?       github.com/F3rreir4L19/juridico-watcher/internal/domain [no test files]
ok      github.com/F3rreir4L19/juridico-watcher/internal/engine 0.992s
ok      github.com/F3rreir4L19/juridico-watcher/internal/pdf    0.791s
ok      github.com/F3rreir4L19/juridico-watcher/internal/service        2.471s
ok      github.com/F3rreir4L19/juridico-watcher/internal/storage        1.097s
ok      github.com/F3rreir4L19/juridico-watcher/internal/watcher        1.207s
PS C:\Users\pc\Documents\juridicwatcher>
O Sprint 6 passou completamente! Vou agora confirmar o sucesso e preparar a entrada no Sprint 7, solicitando o exemplo de procuração para o teste E2E, conforme havíamos combinado.

O Sprint 6 está 100% concluído — todos os pacotes passaram! 🎉

✅ Progresso atualizado
Sprint	Descrição	Status
0	Estrutura, deps, helpers	✅
1	Domínio + Storage	✅
2	Extração PDF + Extractor	✅
3	Evaluator + Interpolator + Actions	✅
4	Pipeline completo	✅
5	Watcher (fsnotify + estabilizador + scanner)	✅
6	Camada de Services (orquestração)	✅
7	Teste E2E com exemplo real	🔲 Próximo
8–9–10	UI + polimento + build	🔲
🎯 Próximo: Sprint 7 — Teste E2E
Agora preciso que você forneça o texto de exemplo de uma procuração (ou documento jurídico equivalente) para eu montar o teste End-to-End.

Por favor, envie algo como:

text
NOME DO OUTORGANTE: João da Silva
TIPO DE PROCURAÇÃO: Plenos Poderes
PODERES: Representar em audiências, assinar contratos, etc.
DATA: 01/01/2024
...
E me diga:

Quais variáveis extrair (ex: nome, tipo_procuracao, poderes).

Quais condições (ex: se tipo_procuracao contém "Plenos" → criar pasta especial).

Como deseja que o arquivo seja organizado (ex: mover para {nome}, renomear para {nome} - {tipo_procuracao}.pdf).

Com isso, criarei o teste E2E completo do Sprint 7.

