conversa com o claude

Você disse: quanto a stack basica, a interface não sei exatamente como faria porque não programo em go para conhecer bibliotecas e frameworks de gui, mas queria que fosse …

quero que me ajude a montar um programa em go. é o seguinte: vai ser um programa simples com uma interface simples, mas eficaz e robusto. ele vai possuir uma tela monitorar onde voce pode definir pastas para ele monitorar. voce clica em adicionar, aparece um campo pra voce colocar o nome daquela r

pasted

quanto a stack basica, a interface não sei exatamente como faria porque não programo em go para conhecer bibliotecas e frameworks de gui, mas queria que fosse escrito em go, o banco de dados em sqlite e se possivel funcionasse em windows e linux. primeiro estruture a ideia melhor, ajude a melhorar, polir, definir pontos importantes, pontuar lacunas a serem definidas, estrutura do projeto, estrutura de pastas, stack e o motivo da escolha e etc. depois, vamos montar uma serie de testes antes de qualquer implementação do programa que vão definir se o programa esta sendo bem implementado ou não, se os requisitos do programa estão sendo satisfeitos da maneira certa ou não, se algum erro pode vir a acontecer ou não, se algum bug foi gerado ou não, meio que estabelecer padrões minimos para o projeto, esses testes vão dizer se estamos indo na direção certa. depois, passaremos para a fase de implementação. se possivel, gostaria que montassemos o programa separando por sprints tudo o que temos que fazer, com mais ou menos tarefas dependendo do nivel de profundidade e complexidade do que deve ser feito na sprint. não passe nada compilado para mim, mas entregue codigo e conteudo e instruções claras e diretas sobre o que fazer com o que foi entregue. vamos começar.

Mostrar mais
5/05
Claude respondeu: Excelente projeto, Levi.
Excelente projeto, Levi. Antes de começar a estruturar, deixa eu fazer algumas perguntas importantes que vão moldar decisões fundamentais da arquitetura. Depois disso, monto a estruturação completa.

Você disse: P: Como o programa vai obter o texto OCR dos documentos?
P: Como o programa vai obter o texto OCR dos documentos?
R: Os PDFs já vêm com camada de texto (OCR feito antes)

P: Quais formatos de arquivo o programa precisa processar?
R: Apenas PDF

P: Qual é a prioridade entre essas características da GUI?
R: Classificação: 1. Nativa e leve (binário pequeno, sem deps), 2. Bonita e moderna (visual web-like), 3. Simples de manter (menos código GUI), 4. Tanto faz, escolha você

5/05
Claude respondeu: Perfeito.
Perfeito. Com essas respostas eu consigo fechar a stack. Vou estruturar tudo agora — primeiro a parte conceitual (refinando a ideia, apontando lacunas, definindo decisões), depois estrutura do projeto, depois a suíte de testes que vai guiar a implementação, e por fim o plano de sprints.

PARTE 1 — Refinamento da ideia
Vou organizar o que você descreveu, apontar onde achei pontos fortes, onde achei lacunas, e propor decisões. Onde tiver decisão minha, marco com [D] — você confirma ou contesta antes de seguir.

1.1. Modelo conceitual
Pelo que entendi, o programa tem três entidades principais:

Pasta Monitorada (Watch) — um caminho do sistema de arquivos com um nome amigável e um flag ativo/inativo. É o "onde olhar".

Regra (Rule) — define "o que fazer quando um documento aparecer". Uma regra tem:

Nome
Lista de pastas monitoradas onde ela se aplica (referenciadas por nome)
Lista de extrações (os "scrapings" — delimitador início + delimitador fim → variável)
Condições (ex: tipo = "procuração")
Lista de ações sequenciais (criar pasta, mover, renomear), com interpolação de variáveis via {nome_var}
Documento processado (ProcessedDoc) — registro de que tal arquivo já foi avaliado por tal regra, para evitar reprocessamento e dar histórico. Falo mais disso adiante.

Isso bate com o que você descreveu. As três formas de disparo (scan inicial ao adicionar pasta, watch contínuo, botão "atualizar" global ou por regra) também ficam claras.

1.2. Lacunas que preciso apontar
Estas são coisas que sua descrição não cobriu e que vão dar dor de cabeça depois se não forem decididas agora:

(a) Quando uma regra "se aplica" a um documento? Sua descrição mistura duas coisas: a regra é aplicável à pasta (sempre), mas o disparo das ações depende da condição (if tipo = procuração). Proponho separar formalmente:

Aplicabilidade: a regra roda extração em todo PDF que apareça nas pastas listadas dela.
Match: as ações só executam se as condições baterem após a extração.
Se nenhuma condição for definida, a regra sempre executa após extrair as variáveis. [D]
(b) E se múltiplas regras forem aplicáveis ao mesmo documento na mesma pasta? Cenários possíveis:

Só executar a primeira que der match (ordem de prioridade).
Executar todas que derem match.
Bloquear cadastro de regras conflitantes.
Proponho: executar todas que derem match, na ordem de criação, mas se uma regra já moveu o arquivo, as próximas são puladas (porque o caminho original não existe mais). Adiciono um campo opcional "prioridade" na regra para o usuário controlar a ordem. [D]

(c) Como evitar reprocessar o mesmo documento? Imprescindível ter uma tabela processed_documents com hash do conteúdo (SHA-256) + caminho + regra + timestamp. Sem isso, se o programa reiniciar, ele reprocessa tudo. Também serve de auditoria.

(d) O que fazer quando a ação falha no meio? Ex: "criar pasta" funciona, "mover" falha porque já existe arquivo com mesmo nome no destino. Proponho:

Cada regra executa de forma transacional do ponto de vista lógico: se uma ação falha, as ações já feitas não são revertidas (não dá para desfazer um mkdir de boa em FS), mas o erro é logado, o documento é marcado como failed no banco, e o usuário vê na UI.
Política de colisão de arquivo: renomear com sufixo ({nome} (2).pdf) por padrão, configurável por regra mais tarde. [D]
(e) Watch contínuo: como detectar "o arquivo terminou de ser escrito"? Esse é um clássico — se o scanner está copiando um PDF de 30MB, o evento de "criado" dispara antes do arquivo estar completo. Proponho: ao detectar arquivo novo, aguardar estabilidade de tamanho (verificar tamanho a cada 500ms, processar quando o tamanho não muda por 2 ciclos consecutivos). [D]

(f) Extração de texto do PDF. Você confirmou que os PDFs já vêm com camada de texto, então não vamos rodar Tesseract. Vou usar a biblioteca github.com/ledongthuc/pdf ou github.com/dslipak/pdf para extrair texto puro. Se um PDF chegar sem camada de texto, ele falha na extração e o documento é marcado como "sem texto extraível" — não tentamos OCR. [D]

(g) Delimitadores: case-sensitive? Múltiplas ocorrências? Regex? Proponho começar simples:

Case-insensitive por padrão (advogados não vão se preocupar com isso), com opção futura de marcar como case-sensitive.
Pega a primeira ocorrência do delimitador início, e a primeira ocorrência do delimitador fim depois dele.
Sem regex no v1 — só matching literal de string. Regex fica para v2 se você quiser. [D]
Delimitador início vazio = começo do texto. Delimitador fim vazio = fim do texto. (Conforme você descreveu.)
Se o delimitador início não for encontrado: a variável fica vazia (não é erro).
Espaços em branco no resultado são trimados (strings.TrimSpace).
(h) Condições nas ações: que operadores? No seu exemplo você usou tipo = procuração. Proponho começar com:

= (igual, case-insensitive)
!= (diferente)
contém / não contém
Combinação por E (AND). Sem OU no v1 para manter simples — se precisar de OU, cria duas regras.
(i) E quando o usuário desativa uma pasta monitorada? O watcher dela para; ela não é varrida em scans nem watch. As regras que apontam para ela continuam válidas mas inativas até a pasta voltar.

(j) E quando o usuário deleta uma pasta monitorada que está em regras? Bloquear deleção e mostrar quais regras dependem dela, ou deletar em cascata e limpar das regras. Proponho: bloquear e listar as dependências, forçando o usuário a editar/deletar as regras primeiro. Mais seguro. [D]

(k) Recursividade: monitora subpastas? Crítico definir. Proponho sim, recursivo por padrão, com checkbox para desativar. Senão fica frustrante. [D]

(l) O programa precisa rodar em segundo plano (tray icon) ou só com janela aberta? Proponho v1 só com janela aberta (mais simples, foco no core). System tray fica para v2. [D]

1.3. Stack escolhida e por quê
Com base nas suas prioridades (1º nativa/leve, 2º bonita, 3º simples de manter):

Linguagem: Go (definido).

GUI: Fyne (fyne.io/fyne/v2).

Por quê: é nativa em Go puro, gera binário único, multiplataforma (Windows e Linux funcionam de fábrica), API limpa, comunidade ativa. Atende o critério #1 (leve, sem deps externas em runtime — o Fyne renderiza via OpenGL).
Alternativa considerada: Wails (web-like, atende critério #2 melhor) — mas embute um runtime de webview e exige toolchain de frontend (Node/npm/etc). Fere o critério #1 que você priorizou primeiro. Descartado.
Outra alternativa: Gio — mais novo e leve, mas tem menos componentes prontos e curva de aprendizado maior, fere o critério #3. Descartado.
Banco: SQLite via modernc.org/sqlite (driver puro em Go, sem dependência de CGO).

Por quê: o driver mattn/go-sqlite3 é o mais popular mas exige CGO, o que complica cross-compile Windows/Linux. O modernc.org/sqlite é Go puro, transpila SQLite C para Go, e o cross-compile fica trivial.
Watcher de filesystem: github.com/fsnotify/fsnotify — padrão de facto da comunidade Go, suporta Windows (ReadDirectoryChangesW) e Linux (inotify) de forma transparente.

Extração de texto de PDF: github.com/ledongthuc/pdf — Go puro, sem deps, suficiente para PDFs com camada de texto. Se mostrar limitação na prática, trocamos por github.com/dslipak/pdf (fork mais ativo).

Acesso ao banco: database/sql stdlib + queries SQL diretas. Sem ORM. Para um projeto com ~5 tabelas, ORM é overkill e adiciona magia.

Migrations: github.com/golang-migrate/migrate ou simplesmente um pacote interno que aplica arquivos .sql numerados. Para v1 vou propor o caminho interno (mais leve), trocamos depois se ficar bagunçado.

Logs: log/slog (stdlib desde Go 1.21) — JSON estruturado, sem deps.

Testes: stdlib testing + github.com/stretchr/testify para asserções legíveis.

1.4. Como o motor de regras funciona — fluxo completo
Pra ficar 100% claro antes de implementar:

Vou seguir só com texto então — fica mais útil pra você acompanhar a lógica.

Quando um documento é detectado (qualquer um dos três triggers — scan inicial, watch, botão atualizar), o pipeline é:

Filtrar: o arquivo é PDF? Senão, ignora.
Estabilizar: aguarda tamanho parar de mudar (só no fluxo de watch).
Hash: calcula SHA-256 do conteúdo. Consulta processed_documents — se já foi processado por todas as regras aplicáveis, pula.
Extrair texto: lê o PDF, extrai texto puro.
Listar regras aplicáveis: busca todas as regras que apontam para a pasta onde o arquivo está, ordenadas por prioridade.
Para cada regra:
Aplica todas as extrações (delimitador início + fim → variável).
Avalia condições. Se não bater, marca como "avaliada, sem match" e segue.
Se bater, executa as ações em sequência interpolando {var}.
Registra resultado (success / failed / skipped) em processed_documents.
Se uma regra moveu o arquivo: as regras seguintes são puladas (porque o caminho mudou), mas isso fica logado.
1.5. Estrutura de pastas do projeto
Convenção Go padrão (Project Layout), adaptada ao tamanho do projeto:

juridico-watcher/
├── cmd/
│   └── juridico-watcher/
│       └── main.go                  # entry point: inicializa DI e abre GUI
├── internal/
│   ├── domain/                      # tipos puros, sem deps externas
│   │   ├── watch.go                 # struct Watch
│   │   ├── rule.go                  # struct Rule, Extraction, Condition, Action
│   │   ├── document.go              # struct Document, ProcessedDoc
│   │   └── errors.go                # erros de domínio
│   ├── storage/                     # camada de persistência
│   │   ├── sqlite.go                # abre conexão, aplica migrations
│   │   ├── migrations/
│   │   │   ├── 001_initial.sql
│   │   │   └── 002_xxx.sql
│   │   ├── watch_repo.go            # CRUD de watches
│   │   ├── rule_repo.go             # CRUD de rules
│   │   └── processed_repo.go        # CRUD de processed_documents
│   ├── pdf/                         # extração de texto
│   │   └── extractor.go
│   ├── engine/                      # motor de regras (coração do sistema)
│   │   ├── engine.go                # orquestra: recebe arquivo, processa
│   │   ├── extractor.go             # aplica scrapings, retorna mapa de variáveis
│   │   ├── evaluator.go             # avalia condições
│   │   ├── interpolator.go          # substitui {var} em strings
│   │   ├── actions.go               # executa CreateFolder, Move, Rename
│   │   └── pipeline.go              # pipeline completo de um documento
│   ├── watcher/                     # observador de filesystem
│   │   ├── watcher.go               # wrapper sobre fsnotify
│   │   ├── stabilizer.go            # aguarda arquivo parar de crescer
│   │   └── scanner.go               # scan recursivo de uma pasta
│   ├── service/                     # camada de aplicação (orquestra storage + engine)
│   │   ├── watch_service.go         # adicionar/remover/ativar pastas
│   │   ├── rule_service.go          # CRUD de regras
│   │   └── scan_service.go          # botão "atualizar" global e por regra
│   └── ui/                          # tudo de Fyne
│       ├── app.go                   # janela principal, abas
│       ├── tab_watches.go           # aba Monitorar
│       ├── tab_rules.go             # aba Regras
│       ├── dialog_rule.go           # diálogo de criar/editar regra
│       ├── dialog_watch.go          # diálogo de criar/editar pasta
│       └── components/              # widgets reutilizáveis (tabela, etc)
├── test/
│   ├── fixtures/                    # PDFs de teste (gerados ou commitados)
│   └── integration/                 # testes end-to-end
├── go.mod
├── go.sum
├── README.md
└── Makefile                         # build, test, run
Pontos da estrutura:

internal/: tudo aqui é privado ao módulo, ninguém pode importar de fora. Isso reforça que é um app, não uma lib.
domain/ não importa nada de outras pastas internas — é o núcleo puro. Facilita teste.
storage/, engine/, watcher/ dependem de domain/ mas não entre si (ou só em uma direção).
service/ orquestra os outros. É chamado pela UI.
ui/ depende só de service/ e domain/. Nunca toca em storage direto.
Isso é a famosa "arquitetura em camadas" — domain no centro, infra fora, UI por último. Garante que dê pra testar o motor sem subir GUI nem banco real.

Antes de eu seguir pra Parte 2 (testes) e Parte 3 (sprints), quero sua confirmação sobre as decisões marcadas com [D], porque elas afetam diretamente o que vamos testar:

[D-a] Aplicabilidade vs Match separados; sem condição = sempre executa após extrair?
[D-b] Múltiplas regras: todas que derem match em ordem, parando se arquivo for movido. Campo "prioridade" na regra?
[D-d] Política de colisão: renomear com sufixo (2), (3)?
[D-e] Estabilização: tamanho estável por 2 ciclos de 500ms?
[D-f] Sem fallback de OCR — PDF sem texto = falha registrada?
[D-g] Delimitadores literais, case-insensitive, primeira ocorrência, sem regex no v1?
[D-j] Bloquear deleção de pasta usada em regras (forçar editar/deletar regras primeiro)?
[D-k] Watch recursivo por padrão (com opção de desativar)?
[D-l] Sem system tray no v1, só janela?
Pode responder em formato livre — "todos confirmados" basta se concordar com tudo, ou pode contestar item por item.

Você disse: concordo com todas as suas sugestões.
concordo com todas as suas sugestões. analisei tudo o que disse e estou de acordo

5/05
Claude respondeu: Beleza, todas as decisões fechadas.
Beleza, todas as decisões fechadas. Agora vou para a Parte 2 — a suíte de testes que vai guiar a implementação.

A ideia aqui é estabelecer o contrato do programa antes de escrever o programa. Esses testes não vão todos passar de cara (óbvio, não tem código ainda), mas eles definem o "como sabemos que está certo". Conforme implementarmos sprints, eles vão ficando verdes.

PARTE 2 — Suíte de testes
Vou organizar os testes em três camadas: unitários (testam funções puras, sem I/O), de integração (testam camadas inteiras com banco real e filesystem real, mas em diretório temporário), e end-to-end (simulam o fluxo todo, do arquivo aparecendo na pasta até a ação executada).

A estrutura dos arquivos de teste segue a convenção Go: cada xxx.go tem seu xxx_test.go ao lado. Os testes de integração e e2e ficam em test/integration/ para deixar claro que são lentos.

2.1. Filosofia dos testes
Três princípios:

Cada teste é um requisito. Se um teste não corresponde a algo que você ou eu definimos, ele não deveria existir. E vice-versa: se um requisito não tem teste, ele vai falhar silenciosamente.
Setup explícito, sem mágica. Nada de mocks elaborados. Usamos diretórios temporários reais (t.TempDir()) e SQLite em arquivo temporário. É mais lento, mas o teste fica óbvio de ler.
Nome do teste descreve o comportamento esperado. TestEngine_QuandoCondicaoNaoBate_NaoExecutaAcoes é melhor que TestEvaluator2.
Vou escrever os testes em português nos nomes porque o domínio é em português (procuração, outorgante, etc.) e fica mais legível pra você. Comentários em inglês conforme convenção Go quando for código-fonte real, mas aqui são testes — vale a pena priorizar legibilidade.

2.2. Convenções e helpers
Antes dos testes em si, definimos um arquivo de helpers em test/testhelpers/ que vai ser usado por todos. Crie esse arquivo:

test/testhelpers/helpers.go

go
package testhelpers

import (
	"database/sql"
	"os"
	"path/filepath"
	"testing"

	"github.com/stretchr/testify/require"
)

// TempDB cria um SQLite em arquivo temporário e retorna a conexão.
// O arquivo é deletado quando o teste termina.
func TempDB(t *testing.T) *sql.DB {
	t.Helper()
	dir := t.TempDir()
	dbPath := filepath.Join(dir, "test.db")
	// IMPORTANTE: ainda não temos a função storage.Open — esta linha
	// deve apontar para ela quando o pacote storage existir.
	// Por ora, deixar como placeholder a ser implementado na Sprint 1.
	_ = dbPath
	return nil // substituir por storage.Open(dbPath) na Sprint 1
}

// WritePDF cria um PDF simples com o texto fornecido.
// Usa uma biblioteca mínima de geração de PDF para os testes.
// Implementação detalhada será feita na Sprint 0 (preparação).
func WritePDF(t *testing.T, dir, filename, textContent string) string {
	t.Helper()
	path := filepath.Join(dir, filename)
	// Placeholder: na Sprint 0 vamos usar github.com/jung-kurt/gofpdf
	// para gerar PDFs de teste com camada de texto.
	err := os.WriteFile(path, []byte(textContent), 0644)
	require.NoError(t, err)
	return path
}

// AssertFileExists falha se o arquivo não existe.
func AssertFileExists(t *testing.T, path string) {
	t.Helper()
	_, err := os.Stat(path)
	require.NoError(t, err, "arquivo deveria existir: %s", path)
}

// AssertFileNotExists falha se o arquivo existe.
func AssertFileNotExists(t *testing.T, path string) {
	t.Helper()
	_, err := os.Stat(path)
	require.True(t, os.IsNotExist(err), "arquivo NÃO deveria existir: %s", path)
}
Esse arquivo é stub agora — preenchemos as funções conforme as sprints avançam. Mas já estabelece a interface dos helpers.

2.3. Testes unitários
Vou listar os arquivos de teste por camada. Você pode criar todos esses arquivos vazios já no início do projeto (com package xxx_test e nada mais), e ir preenchendo conforme as sprints. Para cada arquivo, listo os testes que ele deve conter, com nome e descrição do que verifica.

internal/engine/extractor_test.go — extração de variáveis
go
package engine_test
Testes a implementar:

Teste	O que verifica
TestExtractor_DelimitadorInicioEFim_ExtraiVariavelEntreEles	Caso feliz: texto contém ambos delimitadores, variável recebe o conteúdo entre eles trimado
TestExtractor_DelimitadorInicioVazio_ComecaNoInicioDoTexto	Delimitador início vazio → começa do índice 0
TestExtractor_DelimitadorFimVazio_VaiAteOFimDoTexto	Delimitador fim vazio → vai até o fim
TestExtractor_AmbosVazios_VariavelRecebeTextoCompleto	Ambos vazios → variável recebe o texto inteiro
TestExtractor_DelimitadorInicioNaoEncontrado_VariavelFicaVazia	Não acha início → variável vazia (sem erro)
TestExtractor_DelimitadorFimNaoEncontrado_VariavelFicaVazia	Acha início mas não fim → variável vazia
TestExtractor_CaseInsensitive_EncontraDelimitadorComCaseDiferente	Texto tem "Outorgante:", delimitador é "outorgante:" → bate
TestExtractor_PrimeiraOcorrencia_IgnoraOcorrenciasPosteriores	Texto tem "outorgante:" duas vezes → pega só o primeiro
TestExtractor_TrimaEspacosEmBranco	Conteúdo entre delimitadores é " fulano " → variável recebe "fulano"
TestExtractor_MultiplasExtracoesNaMesmaRegra_TodasAplicadas	Regra com 3 extrações → todas as variáveis são preenchidas
TestExtractor_TextoVazio_TodasVariaveisVazias	PDF vazio → todas variáveis vazias, sem panic
TestExtractor_DelimitadorContemQuebraDeLinha_FuncionaCorretamente	Documento real tem \n — delimitadores podem cruzar linhas
internal/engine/evaluator_test.go — avaliação de condições
Teste	O que verifica
TestEvaluator_OperadorIgual_Match	tipo = "procuração" com variável tipo="procuração" → true
TestEvaluator_OperadorIgual_CaseInsensitive	tipo = "Procuração" com variável="procuração" → true
TestEvaluator_OperadorDiferente_Match	tipo != "outro" com tipo="procuração" → true
TestEvaluator_OperadorContem_Match	nome contém "Silva" em "João da Silva" → true
TestEvaluator_OperadorNaoContem_Match	nome não contém "Silva" em "João Souza" → true
TestEvaluator_MultiplasCondicoesComAND_TodasDevemBater	Duas condições, ambas batem → true
TestEvaluator_MultiplasCondicoesComAND_UmaFalha_RetornaFalse	Duas condições, uma não bate → false
TestEvaluator_VariavelInexistente_TratadaComoStringVazia	Condição usa variável que não foi extraída → string vazia
TestEvaluator_SemCondicoes_RetornaTrue	Regra sem condições → sempre executa (decisão D-a)
internal/engine/interpolator_test.go — substituição de variáveis em strings
Teste	O que verifica
TestInterpolator_VariavelSimples_Substitui	"pasta {nome}" com nome="João" → "pasta João"
TestInterpolator_MultiplasVariaveis_TodasSubstituidas	"{tipo} - {nome}" → "procuração - João"
TestInterpolator_VariavelInexistente_DeixaPlaceholder	"{indefinida}" → loga warning e retorna string vazia ou o placeholder literal — decidir aqui: proponho deixar string vazia e logar warning
TestInterpolator_TextoLiteralSemVariaveis_RetornaInalterado	"texto fixo" → "texto fixo"
TestInterpolator_ChavesEscapadas_NaoSaoInterpoladas	Caso quiséssemos escapar — proponho não suportar escape no v1, chaves no texto literal não acontecem em nomes de pastas/arquivos
TestInterpolator_VariavelComCaracteresInvalidosParaPath_Sanitiza	nome="João/Silva" → / é substituído por _ antes de virar nome de pasta. Crítico para evitar criar pastas indevidas.
internal/engine/actions_test.go — execução de ações
Teste	O que verifica
TestAction_CreateFolder_CriaPastaComNomeInterpolado	Ação cria pasta dir/{nome} corretamente
TestAction_CreateFolder_PastaJaExiste_NaoFalha	Idempotente: rodar 2x não dá erro
TestAction_Move_MoveArquivoParaDestino	Arquivo origem some, aparece no destino
TestAction_Move_DestinoNaoExiste_CriaCaminhoIntermediario	Move para a/b/c/file.pdf mesmo se a/b/c não existir → cria recursivamente
TestAction_Move_ColisaoDeNome_RenomeiaComSufixo	Já existe file.pdf no destino → vira file (2).pdf (decisão D-d)
TestAction_Move_ColisaoMultipla_IncrementaNumero	Já existe (2) e (3) → vira (4)
TestAction_Rename_RenomeiaArquivoMantendoExtensao	Renomeia "scan001.pdf" para "{nome}.pdf" → preserva .pdf
TestAction_Rename_NomeContemBarras_FalhaComErroClaro	Nome interpolado tem / → erro explícito (proteção)
TestAction_SequenciaCompleta_CriaPastaMoveERenomeia	Roda as 3 ações em sequência, estado final correto
TestAction_AcaoFalha_DocumentoMarcadoComoFailed	Move falha (permissão) → retorna erro, pipeline registra failed (decisão D-d)
internal/engine/pipeline_test.go — pipeline completo de um documento
Teste	O que verifica
TestPipeline_DocumentoSemRegrasAplicaveis_NaoFazNada	Pasta não tem regras → arquivo continua intacto
TestPipeline_RegraAplicavelMasCondicaoNaoBate_NaoExecutaAcoes	Extrai variáveis, avalia, falha condição → arquivo intacto, registra "evaluated_no_match"
TestPipeline_RegraAplicavelECondicaoBate_ExecutaAcoes	Caso feliz: variáveis extraídas, condição true, ações rodadas
TestPipeline_DuasRegrasAplicaveis_AmbasExecutam	Decisão D-b: ambas rodam em ordem de prioridade
TestPipeline_PrimeiraRegraMoveuArquivo_SegundaRegraEhPulada	Decisão D-b: arquivo não está mais no caminho original → log e skip
TestPipeline_DocumentoJaProcessadoPelaMesmaRegra_NaoReprocessa	Hash do arquivo + ID da regra já em processed_documents → skip
TestPipeline_PDFSemTextoExtraivel_RegistraFailedEPara	Decisão D-f: PDF imagem-only → falha registrada, não tenta OCR
TestPipeline_ArquivoNaoEhPDF_Ignora	.txt, .docx, etc → ignorado silenciosamente
internal/pdf/extractor_test.go
Teste	O que verifica
TestPDFExtractor_PDFComCamadaDeTexto_ExtraiCorretamente	Usa fixture com texto conhecido, compara
TestPDFExtractor_PDFCorrupto_RetornaErro	Bytes inválidos → erro, não panic
TestPDFExtractor_PDFSemTexto_RetornaStringVaziaSemErro	PDF de imagem pura → string vazia, mas não falha (a falha é registrada no pipeline)
TestPDFExtractor_PDFMultipagina_ConcatenaTextoDeTodasPaginas	Doc com 3 páginas → texto contém de todas
internal/storage/watch_repo_test.go, rule_repo_test.go, processed_repo_test.go
Para cada repositório, padrão:

Teste	O que verifica
TestWatchRepo_Create_PersisteEDevolveID	Insert básico
TestWatchRepo_GetByID_RetornaWatchSalvo	Read
TestWatchRepo_GetByName_RetornaWatchSalvo	Read por nome (necessário para resolver nomes em regras)
TestWatchRepo_GetByName_NaoExiste_RetornaErrNotFound	Erro tipado
TestWatchRepo_List_RetornaTodosOrdenadosPorNome	List
TestWatchRepo_Update_PersisteAlteracoes	Update
TestWatchRepo_Delete_Remove	Delete
TestWatchRepo_Delete_ComRegrasReferenciando_RetornaErro	Decisão D-j
TestWatchRepo_NomeUnico_CreateDuplicado_Falha	Constraint UNIQUE em nome
Padrão similar para rule_repo e processed_repo.

internal/watcher/stabilizer_test.go
Teste	O que verifica
TestStabilizer_ArquivoTamanhoConstante_RetornaImediatamenteNoSegundoCiclo	Decisão D-e
TestStabilizer_ArquivoCrescendo_AguardaParar	Simula crescimento durante 3 ciclos, depois para → estabiliza
TestStabilizer_TimeoutMaximo_DesistirAposNCiclos	Proteção contra arquivo crescendo infinitamente — timeout de 30s
2.4. Testes de integração
Esses ficam em test/integration/. Cada teste sobe um SQLite real e usa diretórios temporários reais.

test/integration/watch_lifecycle_test.go
Teste	O que verifica
TestIntegracao_AdicionarPasta_DispararScanInicial	Adiciona pasta com PDFs preexistentes → scan roda → arquivos processados
TestIntegracao_AdicionarPastaSemRegras_NaoFazNada	Cenário do seu exemplo: pasta nova, nenhuma regra ainda → scan roda mas não executa nada
TestIntegracao_DesativarPasta_ParaDeMonitorar	Desativar → adicionar arquivo novo → não é processado
TestIntegracao_ReativarPasta_VoltaAMonitorarMasNaoFazScanRetroativo	Reativar → arquivo novo é processado, mas arquivos que apareceram durante inativo NÃO são (a menos que clique "atualizar")
test/integration/rule_lifecycle_test.go
Teste	O que verifica
TestIntegracao_CriarRegra_RodaContraDocumentosExistentes	Cenário do seu exemplo: cria regra → botão atualizar → encontra docs preexistentes
TestIntegracao_CaminhoEntreAspasNaCriacaoDeRegra_AdicionaPastaAutomaticamente	Criar regra com "/tmp/xyz" → cria Watch chamado path01
TestIntegracao_AtualizarPorRegra_SoRodaAquelaRegra	Outras regras ficam intactas
TestIntegracao_AtualizarGlobal_RodaTodasAsRegrasEmTodasAsPastas	Botão geral
TestIntegracao_DeletarRegra_NaoAfetaDocumentosJaProcessados	Histórico mantido
test/integration/watch_runtime_test.go
Teste	O que verifica
TestIntegracao_NovoArquivoApareceNaPasta_DispararProcessamentoAutomatico	Watch contínuo funciona
TestIntegracao_ArquivoCopiadoLentamente_ProcessadoApenasAposEstabilizar	Decisão D-e funciona end-to-end
TestIntegracao_ArquivoEmSubpasta_ProcessadoQuandoRecursivoLigado	Decisão D-k
TestIntegracao_ArquivoEmSubpasta_IgnoradoQuandoRecursivoDesligado	Decisão D-k inversa
TestIntegracao_VariosArquivosSimultaneos_TodosProcessados	Concorrência: 10 arquivos aparecem ao mesmo tempo, todos viram processados
TestIntegracao_ProgramaReinicia_NaoReprocessaArquivosJaProcessados	Persistência de estado entre execuções
2.5. Testes end-to-end
Esses simulam o fluxo do seu exemplo do briefing. Ficam em test/integration/e2e_test.go.

Teste	O que verifica
TestE2E_FluxoCompletoDoExemploProcuracao	O teste mais importante. Reproduz exatamente o cenário que você descreveu: cria pasta digitalizadoras, scan inicial não faz nada, cria regra procuração com extrações de outorgante: e pelo, ações criar pasta + mover + renomear, clica atualizar, verifica que PDFs preexistentes foram movidos para pastas com nome do outorgante e renomeados
TestE2E_DocumentoNaoMatcheRegra_FicaIntacto	Mesmo cenário, mas PDF é uma "petição" (tipo != procuração) → arquivo não é tocado
Esse TestE2E_FluxoCompletoDoExemploProcuracao é o nosso "norte". Quando ele passar, sabemos que o motor está funcional.

2.6. O que NÃO vamos testar (e por quê)
Importante explicitar pra não gastar tempo à toa:

GUI Fyne: testar GUI Fyne automatizado é doloroso e tem retorno baixo. Vamos testar manualmente conforme implementamos. A lógica fica em service/, que é testada — a UI só renderiza.
Performance: não vamos ter benchmarks formais no v1. Se houver problema, a gente mede pontualmente.
Cross-platform: confiamos que fsnotify, modernc.org/sqlite e Fyne são multiplataforma. Testes rodam no Linux do CI; quando você for testar em Windows, faz manualmente.
2.7. Plano de criação dos arquivos de teste
Antes de qualquer código de produção, sua tarefa será:

Sprint 0 (preparação) — você cria a estrutura:

Cria a pasta do projeto e roda go mod init github.com/leviferreira/juridico-watcher (ajuste o path conforme seu github).
Cria todos os diretórios listados na estrutura (Parte 1.5).
Cria todos os arquivos _test.go listados acima, vazios — apenas com a declaração package xxx_test no topo. Isso documenta o escopo desde o primeiro commit.
Cria o test/testhelpers/helpers.go com o stub que mostrei.
Adiciona dependências mínimas:
   go get github.com/stretchr/testify
   go get fyne.io/fyne/v2
   go get modernc.org/sqlite
   go get github.com/fsnotify/fsnotify
   go get github.com/ledongthuc/pdf
   go get github.com/jung-kurt/gofpdf
(gofpdf é só pra gerar fixtures de PDF nos testes.) 6. Roda go mod tidy. 7. Cria um Makefile simples:

makefile
   .PHONY: test test-unit test-integration build run
   test: test-unit test-integration
   test-unit:
   	go test ./internal/...
   test-integration:
   	go test ./test/integration/...
   build:
   	go build -o bin/juridico-watcher ./cmd/juridico-watcher
   run:
   	go run ./cmd/juridico-watcher
Quando terminar, confirma comigo, e seguimos para a Parte 3 — o plano de sprints. Ali eu vou definir, sprint a sprint, exatamente quais testes ficam verdes ao final de cada uma e qual código produz isso. Cada sprint vai entregar uma fatia testável do programa, em ordem que minimiza retrabalho (domain → storage → engine → watcher → service → ui).

Antes de seguir, três perguntas pra fechar a Parte 2:

Confere o nome do módulo Go (github.com/leviferreira/juridico-watcher) — quer outro? Pode ser local tipo juridico-watcher se não vai pro GitHub.
Nome do programa em si — vou usar "juridico-watcher" como placeholder, mas se quiser outro nome (pra binário, janela, etc.), me diz.
Sobre o teste do interpolador com variável inexistente: confirma que prefere "string vazia + warning no log" em vez de "deixar {indefinida} literal"? A primeira opção é mais segura (evita criar pastas com nome {xxx} no FS); a segunda é mais explícita pro usuário ver que algo deu errado.
Você disse: o nome esta ótimo, podemos seguir.
o nome esta ótimo, podemos seguir. sobre o test do interpolador com variavel inexistente, gostei da abordagem sugerida por voce, podemos seguir sim.

5/05
Claude respondeu: Beleza.
Beleza. Vamos pra Parte 3 — o plano de sprints.

PARTE 3 — Plano de Sprints
A ordem das sprints segue a regra "do núcleo pra fora": começamos pelo que não depende de nada (domínio puro), depois persistência, depois motor de regras, depois watcher, depois orquestração, e por último GUI. Cada sprint deixa um conjunto específico de testes verdes e tem uma "definition of done" clara.

Filosofia: ao fim de cada sprint, make test-unit deve passar 100%. Testes de integração e E2E vão ficando verdes progressivamente. Se uma sprint quebrar testes de sprints anteriores, ela não terminou.

Visão geral das sprints
Sprint	Foco	Testes que ficam verdes
0	Preparação: estrutura, deps, fixtures	Nenhum (só compila)
1	Domain types + storage (SQLite, repos)	Todos os *_repo_test.go
2	Extração de PDF + extractor de variáveis	pdf/extractor_test.go, engine/extractor_test.go
3	Evaluator + Interpolator + Actions	evaluator_test.go, interpolator_test.go, actions_test.go
4	Pipeline (junta tudo do engine)	engine/pipeline_test.go
5	Watcher (fsnotify + stabilizer + scanner)	watcher/stabilizer_test.go + integração de watcher
6	Services (orquestram storage + engine + watcher)	Testes de integração watch_lifecycle e rule_lifecycle
7	E2E e teste do fluxo do exemplo procuração	TestE2E_FluxoCompletoDoExemploProcuracao
8	UI Fyne — aba Monitorar	(manual)
9	UI Fyne — aba Regras (com extrações e ações)	(manual)
10	UI Fyne — botão Atualizar global e por regra, polish, build cross-platform	(manual)
11 sprints contando a 0. Vou detalhar uma a uma agora — mas pra não estourar tudo numa mensagem só, vou fazer assim: te entrego Sprint 0 e Sprint 1 completas nesta resposta. Quando você terminar a Sprint 1, me diz, e eu entrego a Sprint 2 e 3. E assim por diante.

Isso é melhor por dois motivos: (a) você não fica afogado em texto, (b) se durante a Sprint 1 a gente descobrir algo que muda decisão, eu ajusto as sprints seguintes antes de mandá-las.

SPRINT 0 — Preparação
Objetivo: deixar o esqueleto do projeto pronto pra receber código. Nada de lógica, só estrutura, deps e fixtures.

Tarefas
0.1. Criar o diretório do projeto
bash
mkdir juridico-watcher
cd juridico-watcher
go mod init juridico-watcher
0.2. Criar a estrutura de pastas
Rode esse bloco no terminal (Linux/Mac) ou adapte para PowerShell:

bash
mkdir -p cmd/juridico-watcher
mkdir -p internal/domain
mkdir -p internal/storage/migrations
mkdir -p internal/pdf
mkdir -p internal/engine
mkdir -p internal/watcher
mkdir -p internal/service
mkdir -p internal/ui/components
mkdir -p test/testhelpers
mkdir -p test/integration
mkdir -p test/fixtures
mkdir -p bin
PowerShell equivalente:

powershell
$dirs = @(
  "cmd/juridico-watcher",
  "internal/domain",
  "internal/storage/migrations",
  "internal/pdf",
  "internal/engine",
  "internal/watcher",
  "internal/service",
  "internal/ui/components",
  "test/testhelpers",
  "test/integration",
  "test/fixtures",
  "bin"
)
$dirs | ForEach-Object { New-Item -ItemType Directory -Force -Path $_ }
0.3. Adicionar dependências
bash
go get github.com/stretchr/testify
go get fyne.io/fyne/v2
go get modernc.org/sqlite
go get github.com/fsnotify/fsnotify
go get github.com/ledongthuc/pdf
go get github.com/jung-kurt/gofpdf
go mod tidy
0.4. Criar o Makefile
Makefile (na raiz):

makefile
.PHONY: test test-unit test-integration build run clean fmt vet

test: test-unit test-integration

test-unit:
	go test -race -count=1 ./internal/...

test-integration:
	go test -race -count=1 -timeout 60s ./test/integration/...

test-verbose:
	go test -race -count=1 -v ./internal/... ./test/integration/...

build:
	go build -o bin/juridico-watcher ./cmd/juridico-watcher

build-windows:
	GOOS=windows GOARCH=amd64 go build -o bin/juridico-watcher.exe ./cmd/juridico-watcher

build-linux:
	GOOS=linux GOARCH=amd64 go build -o bin/juridico-watcher ./cmd/juridico-watcher

run:
	go run ./cmd/juridico-watcher

clean:
	rm -rf bin/

fmt:
	go fmt ./...

vet:
	go vet ./...
-race ativa o detector de race conditions (importante pro watcher). -count=1 desabilita cache de teste (queremos sempre rodar de verdade).

0.5. Criar o .gitignore
.gitignore (na raiz):

bin/
*.exe
*.db
*.db-shm
*.db-wal
.idea/
.vscode/
*.log
test/fixtures/generated/
0.6. Criar main.go placeholder
cmd/juridico-watcher/main.go:

go
package main

import "fmt"

func main() {
	fmt.Println("juridico-watcher: not implemented yet")
}
Isso garante que go build funciona desde o dia 1.

0.7. Criar gerador de PDFs de teste
Esse é o helper mais importante da Sprint 0. Os testes vão precisar gerar PDFs com texto previsível. Usamos gofpdf.

test/testhelpers/pdfgen.go:

go
package testhelpers

import (
	"path/filepath"
	"testing"

	"github.com/jung-kurt/gofpdf"
	"github.com/stretchr/testify/require"
)

// WritePDF cria um PDF com o texto fornecido em uma única página.
// Retorna o caminho absoluto do arquivo criado.
//
// O texto é escrito em fonte padrão Arial 12. Quebras de linha (\n) no
// input geram quebras visuais no PDF e ficam preservadas na extração de texto.
func WritePDF(t *testing.T, dir, filename, textContent string) string {
	t.Helper()
	path := filepath.Join(dir, filename)

	pdf := gofpdf.New("P", "mm", "A4", "")
	pdf.AddPage()
	pdf.SetFont("Arial", "", 12)
	// MultiCell escreve com quebra de linha automática e respeita \n
	pdf.MultiCell(0, 6, textContent, "", "L", false)

	err := pdf.OutputFileAndClose(path)
	require.NoError(t, err, "falha ao gerar PDF de teste")
	return path
}

// WriteEmptyPDF cria um PDF vazio (sem texto) — útil para testar
// o caso de PDF sem camada de texto extraível.
func WriteEmptyPDF(t *testing.T, dir, filename string) string {
	t.Helper()
	path := filepath.Join(dir, filename)
	pdf := gofpdf.New("P", "mm", "A4", "")
	pdf.AddPage()
	// nenhum texto adicionado
	err := pdf.OutputFileAndClose(path)
	require.NoError(t, err)
	return path
}

// WriteCorruptPDF cria um arquivo .pdf com bytes inválidos —
// útil para testar tratamento de erro de extração.
func WriteCorruptPDF(t *testing.T, dir, filename string) string {
	t.Helper()
	path := filepath.Join(dir, filename)
	err := writeBytes(path, []byte("isso não é um PDF válido"))
	require.NoError(t, err)
	return path
}
test/testhelpers/helpers.go (substitui o stub anterior):

go
package testhelpers

import (
	"os"
	"testing"

	"github.com/stretchr/testify/require"
)

// AssertFileExists falha se o arquivo não existe.
func AssertFileExists(t *testing.T, path string) {
	t.Helper()
	_, err := os.Stat(path)
	require.NoError(t, err, "arquivo deveria existir: %s", path)
}

// AssertFileNotExists falha se o arquivo existe.
func AssertFileNotExists(t *testing.T, path string) {
	t.Helper()
	_, err := os.Stat(path)
	require.True(t, os.IsNotExist(err), "arquivo NÃO deveria existir: %s", path)
}

// AssertDirExists falha se o diretório não existe.
func AssertDirExists(t *testing.T, path string) {
	t.Helper()
	info, err := os.Stat(path)
	require.NoError(t, err, "diretório deveria existir: %s", path)
	require.True(t, info.IsDir(), "%s deveria ser um diretório", path)
}

func writeBytes(path string, data []byte) error {
	return os.WriteFile(path, data, 0644)
}
0.8. Verificar que tudo compila
bash
make build
make test-unit
make build deve produzir bin/juridico-watcher (ou .exe no Windows). make test-unit não deve achar nenhum teste ainda (é esperado — vai dizer "no test files"), mas não pode dar erro de compilação.

Definition of Done — Sprint 0
 Estrutura de pastas criada conforme Parte 1.5.
 go.mod tem todas as dependências listadas.
 Makefile funcional: make build, make test-unit rodam sem erro.
 bin/juridico-watcher é gerado e imprime "juridico-watcher: not implemented yet".
 test/testhelpers/pdfgen.go e helpers.go compilam.
 Commit inicial feito.
SPRINT 1 — Domain types + Storage
Objetivo: definir os tipos de domínio (structs puras) e a camada de persistência completa em SQLite. Ao final, é possível criar/ler/atualizar/deletar Watches, Rules e ProcessedDocs no banco.

Testes que ficam verdes: todos os *_repo_test.go.

Visão da camada
domain/      (structs puras, sem deps)
   ↑
storage/     (depende de domain, modernc.org/sqlite, database/sql)
Tarefas
1.1. Definir os tipos de domínio
internal/domain/watch.go:

go
package domain

import "time"

// Watch representa uma pasta monitorada pelo sistema.
type Watch struct {
	ID        int64
	Name      string    // nome amigável, único, usado em referências de regras
	Path      string    // caminho absoluto do filesystem
	Active    bool      // se está ativamente sendo monitorada
	Recursive bool      // se monitora subpastas (default true conforme decisão D-k)
	CreatedAt time.Time
	UpdatedAt time.Time
}
internal/domain/rule.go:

go
package domain

import "time"

// Operator representa o tipo de comparação numa Condition.
type Operator string

const (
	OpEquals     Operator = "eq"
	OpNotEquals  Operator = "neq"
	OpContains   Operator = "contains"
	OpNotContain Operator = "not_contains"
)

// ActionType representa o tipo de ação a ser executada.
type ActionType string

const (
	ActionCreateFolder ActionType = "create_folder"
	ActionMove         ActionType = "move"
	ActionRename       ActionType = "rename"
)

// Extraction define como capturar uma variável a partir do texto do documento.
type Extraction struct {
	ID            int64
	RuleID        int64
	VariableName  string // nome da variável, ex: "nome", "tipo"
	StartDelim    string // delimitador de início (vazio = começo do texto)
	EndDelim      string // delimitador de fim (vazio = fim do texto)
	Order         int    // ordem de aplicação (irrelevante para correção mas bom para UI)
}

// Condition define uma comparação que precisa ser verdadeira para a regra disparar.
type Condition struct {
	ID           int64
	RuleID       int64
	VariableName string
	Operator     Operator
	Value        string
	Order        int
}

// Action define uma operação a ser executada quando a regra dá match.
type Action struct {
	ID     int64
	RuleID int64
	Type   ActionType
	// Target é o argumento da ação:
	// - CreateFolder: nome (ou caminho relativo) da pasta a criar
	// - Move: pasta de destino
	// - Rename: novo nome do arquivo (sem extensão; extensão é preservada)
	Target string
	Order  int
}

// Rule é a entidade principal: combina pastas-alvo, extrações, condições e ações.
type Rule struct {
	ID         int64
	Name       string
	Priority   int           // menor = roda primeiro (decisão D-b)
	Active     bool
	WatchIDs   []int64       // IDs de Watches onde esta regra se aplica
	Extractions []Extraction
	Conditions  []Condition
	Actions     []Action
	CreatedAt  time.Time
	UpdatedAt  time.Time
}
internal/domain/document.go:

go
package domain

import "time"

// ProcessingStatus representa o resultado do processamento de um documento por uma regra.
type ProcessingStatus string

const (
	StatusSuccess     ProcessingStatus = "success"
	StatusNoMatch     ProcessingStatus = "no_match"      // regra avaliada mas condição não bateu
	StatusFailed      ProcessingStatus = "failed"        // erro ao executar ação
	StatusSkippedMoved ProcessingStatus = "skipped_moved" // arquivo movido por regra anterior
	StatusNoText      ProcessingStatus = "no_text"       // PDF sem texto extraível
)

// ProcessedDoc registra que um arquivo foi avaliado por uma regra.
// Chave lógica: (FileHash, RuleID).
type ProcessedDoc struct {
	ID          int64
	FileHash    string           // SHA-256 do conteúdo
	OriginalPath string          // caminho onde estava quando foi processado
	RuleID      int64
	Status      ProcessingStatus
	ErrorMsg    string           // preenchido quando Status = failed
	ProcessedAt time.Time
}
internal/domain/errors.go:

go
package domain

import "errors"

var (
	ErrNotFound        = errors.New("entidade não encontrada")
	ErrDuplicateName   = errors.New("nome já existe")
	ErrWatchInUse      = errors.New("pasta monitorada está em uso por regras") // decisão D-j
	ErrInvalidInput    = errors.New("entrada inválida")
)
1.2. Criar a migration inicial
internal/storage/migrations/001_initial.sql:

sql
-- Habilita foreign keys (SQLite não habilita por padrão)
PRAGMA foreign_keys = ON;

CREATE TABLE IF NOT EXISTS watches (
    id          INTEGER PRIMARY KEY AUTOINCREMENT,
    name        TEXT    NOT NULL UNIQUE,
    path        TEXT    NOT NULL,
    active      INTEGER NOT NULL DEFAULT 1,
    recursive   INTEGER NOT NULL DEFAULT 1,
    created_at  TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP,
    updated_at  TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP
);

CREATE INDEX IF NOT EXISTS idx_watches_active ON watches(active);

CREATE TABLE IF NOT EXISTS rules (
    id         INTEGER PRIMARY KEY AUTOINCREMENT,
    name       TEXT    NOT NULL UNIQUE,
    priority   INTEGER NOT NULL DEFAULT 100,
    active     INTEGER NOT NULL DEFAULT 1,
    created_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP
);

CREATE INDEX IF NOT EXISTS idx_rules_active_priority ON rules(active, priority);

-- Relação N:N entre rules e watches
CREATE TABLE IF NOT EXISTS rule_watches (
    rule_id  INTEGER NOT NULL,
    watch_id INTEGER NOT NULL,
    PRIMARY KEY (rule_id, watch_id),
    FOREIGN KEY (rule_id)  REFERENCES rules(id)   ON DELETE CASCADE,
    FOREIGN KEY (watch_id) REFERENCES watches(id) ON DELETE RESTRICT
    -- ON DELETE RESTRICT garante decisão D-j: não dá pra deletar watch se está em regra
);

CREATE INDEX IF NOT EXISTS idx_rule_watches_watch ON rule_watches(watch_id);

CREATE TABLE IF NOT EXISTS extractions (
    id            INTEGER PRIMARY KEY AUTOINCREMENT,
    rule_id       INTEGER NOT NULL,
    variable_name TEXT    NOT NULL,
    start_delim   TEXT    NOT NULL DEFAULT '',
    end_delim     TEXT    NOT NULL DEFAULT '',
    sort_order    INTEGER NOT NULL DEFAULT 0,
    FOREIGN KEY (rule_id) REFERENCES rules(id) ON DELETE CASCADE
);

CREATE INDEX IF NOT EXISTS idx_extractions_rule ON extractions(rule_id);

CREATE TABLE IF NOT EXISTS conditions (
    id            INTEGER PRIMARY KEY AUTOINCREMENT,
    rule_id       INTEGER NOT NULL,
    variable_name TEXT    NOT NULL,
    operator      TEXT    NOT NULL,
    value         TEXT    NOT NULL,
    sort_order    INTEGER NOT NULL DEFAULT 0,
    FOREIGN KEY (rule_id) REFERENCES rules(id) ON DELETE CASCADE
);

CREATE INDEX IF NOT EXISTS idx_conditions_rule ON conditions(rule_id);

CREATE TABLE IF NOT EXISTS actions (
    id         INTEGER PRIMARY KEY AUTOINCREMENT,
    rule_id    INTEGER NOT NULL,
    type       TEXT    NOT NULL,
    target     TEXT    NOT NULL,
    sort_order INTEGER NOT NULL DEFAULT 0,
    FOREIGN KEY (rule_id) REFERENCES rules(id) ON DELETE CASCADE
);

CREATE INDEX IF NOT EXISTS idx_actions_rule ON actions(rule_id);

CREATE TABLE IF NOT EXISTS processed_documents (
    id            INTEGER PRIMARY KEY AUTOINCREMENT,
    file_hash     TEXT    NOT NULL,
    original_path TEXT    NOT NULL,
    rule_id       INTEGER NOT NULL,
    status        TEXT    NOT NULL,
    error_msg     TEXT    NOT NULL DEFAULT '',
    processed_at  TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP,
    FOREIGN KEY (rule_id) REFERENCES rules(id) ON DELETE CASCADE,
    UNIQUE (file_hash, rule_id)
);

CREATE INDEX IF NOT EXISTS idx_processed_hash ON processed_documents(file_hash);
CREATE INDEX IF NOT EXISTS idx_processed_rule ON processed_documents(rule_id);
1.3. Criar o gerenciador de migrations
internal/storage/migrations.go:

go
package storage

import (
	"database/sql"
	"embed"
	"fmt"
	"sort"
	"strings"
)

//go:embed migrations/*.sql
var migrationsFS embed.FS

// applyMigrations roda todas as migrations em ordem.
// Cria a tabela _migrations para rastrear as já aplicadas.
func applyMigrations(db *sql.DB) error {
	if _, err := db.Exec(`
		CREATE TABLE IF NOT EXISTS _migrations (
			name TEXT PRIMARY KEY,
			applied_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP
		)
	`); err != nil {
		return fmt.Errorf("criar _migrations: %w", err)
	}

	entries, err := migrationsFS.ReadDir("migrations")
	if err != nil {
		return fmt.Errorf("ler diretório de migrations: %w", err)
	}

	var names []string
	for _, e := range entries {
		if !e.IsDir() && strings.HasSuffix(e.Name(), ".sql") {
			names = append(names, e.Name())
		}
	}
	sort.Strings(names)

	for _, name := range names {
		var exists int
		err := db.QueryRow(
			"SELECT COUNT(*) FROM _migrations WHERE name = ?", name,
		).Scan(&exists)
		if err != nil {
			return fmt.Errorf("checar migration %s: %w", name, err)
		}
		if exists > 0 {
			continue
		}

		content, err := migrationsFS.ReadFile("migrations/" + name)
		if err != nil {
			return fmt.Errorf("ler migration %s: %w", name, err)
		}

		tx, err := db.Begin()
		if err != nil {
			return fmt.Errorf("iniciar tx para %s: %w", name, err)
		}
		if _, err := tx.Exec(string(content)); err != nil {
			tx.Rollback()
			return fmt.Errorf("executar %s: %w", name, err)
		}
		if _, err := tx.Exec(
			"INSERT INTO _migrations (name) VALUES (?)", name,
		); err != nil {
			tx.Rollback()
			return fmt.Errorf("registrar %s: %w", name, err)
		}
		if err := tx.Commit(); err != nil {
			return fmt.Errorf("commit %s: %w", name, err)
		}
	}

	return nil
}
1.4. Criar o construtor da conexão SQLite
internal/storage/sqlite.go:

go
package storage

import (
	"database/sql"
	"fmt"

	_ "modernc.org/sqlite"
)

// Open abre (ou cria) o banco SQLite no caminho fornecido,
// aplica todas as migrations pendentes e retorna a conexão.
//
// Use ":memory:" como path para um banco em memória (útil para testes).
func Open(path string) (*sql.DB, error) {
	dsn := fmt.Sprintf("%s?_pragma=foreign_keys(1)&_pragma=journal_mode(WAL)", path)
	db, err := sql.Open("sqlite", dsn)
	if err != nil {
		return nil, fmt.Errorf("abrir banco: %w", err)
	}

	// Verifica conectividade imediatamente
	if err := db.Ping(); err != nil {
		db.Close()
		return nil, fmt.Errorf("ping: %w", err)
	}

	if err := applyMigrations(db); err != nil {
		db.Close()
		return nil, fmt.Errorf("aplicar migrations: %w", err)
	}

	return db, nil
}
Detalhes importantes:

_pragma=foreign_keys(1) força FKs (essencial pro ON DELETE RESTRICT funcionar).
_pragma=journal_mode(WAL) melhora concorrência (importante quando watcher e UI lerem ao mesmo tempo).
1.5. Atualizar test/testhelpers/helpers.go para abrir DB
Adicione esta função ao helpers.go:

go
// (adicione no topo do arquivo, junto com os outros imports)
import (
	"database/sql"
	"path/filepath"
	"juridico-watcher/internal/storage"
)

// TempDB cria um SQLite em arquivo temporário com migrations aplicadas.
// O banco é destruído quando o teste termina.
func TempDB(t *testing.T) *sql.DB {
	t.Helper()
	dir := t.TempDir()
	dbPath := filepath.Join(dir, "test.db")
	db, err := storage.Open(dbPath)
	require.NoError(t, err)
	t.Cleanup(func() { db.Close() })
	return db
}
1.6. Implementar WatchRepo
internal/storage/watch_repo.go:

go
package storage

import (
	"database/sql"
	"errors"
	"fmt"
	"strings"
	"time"

	"juridico-watcher/internal/domain"
)

type WatchRepo struct {
	db *sql.DB
}

func NewWatchRepo(db *sql.DB) *WatchRepo {
	return &WatchRepo{db: db}
}

func (r *WatchRepo) Create(w *domain.Watch) error {
	now := time.Now().UTC()
	res, err := r.db.Exec(
		`INSERT INTO watches (name, path, active, recursive, created_at, updated_at)
		 VALUES (?, ?, ?, ?, ?, ?)`,
		w.Name, w.Path, boolToInt(w.Active), boolToInt(w.Recursive), now, now,
	)
	if err != nil {
		if isUniqueConstraint(err) {
			return fmt.Errorf("%w: %s", domain.ErrDuplicateName, w.Name)
		}
		return fmt.Errorf("create watch: %w", err)
	}
	id, err := res.LastInsertId()
	if err != nil {
		return fmt.Errorf("last insert id: %w", err)
	}
	w.ID = id
	w.CreatedAt = now
	w.UpdatedAt = now
	return nil
}

func (r *WatchRepo) GetByID(id int64) (*domain.Watch, error) {
	row := r.db.QueryRow(
		`SELECT id, name, path, active, recursive, created_at, updated_at
		 FROM watches WHERE id = ?`, id,
	)
	return scanWatch(row)
}

func (r *WatchRepo) GetByName(name string) (*domain.Watch, error) {
	row := r.db.QueryRow(
		`SELECT id, name, path, active, recursive, created_at, updated_at
		 FROM watches WHERE name = ?`, name,
	)
	return scanWatch(row)
}

func (r *WatchRepo) List() ([]*domain.Watch, error) {
	rows, err := r.db.Query(
		`SELECT id, name, path, active, recursive, created_at, updated_at
		 FROM watches ORDER BY name ASC`,
	)
	if err != nil {
		return nil, fmt.Errorf("list watches: %w", err)
	}
	defer rows.Close()

	var out []*domain.Watch
	for rows.Next() {
		w, err := scanWatchRows(rows)
		if err != nil {
			return nil, err
		}
		out = append(out, w)
	}
	return out, rows.Err()
}

func (r *WatchRepo) Update(w *domain.Watch) error {
	now := time.Now().UTC()
	res, err := r.db.Exec(
		`UPDATE watches
		 SET name = ?, path = ?, active = ?, recursive = ?, updated_at = ?
		 WHERE id = ?`,
		w.Name, w.Path, boolToInt(w.Active), boolToInt(w.Recursive), now, w.ID,
	)
	if err != nil {
		if isUniqueConstraint(err) {
			return fmt.Errorf("%w: %s", domain.ErrDuplicateName, w.Name)
		}
		return fmt.Errorf("update watch: %w", err)
	}
	rows, _ := res.RowsAffected()
	if rows == 0 {
		return domain.ErrNotFound
	}
	w.UpdatedAt = now
	return nil
}

func (r *WatchRepo) Delete(id int64) error {
	// Verifica se há regras referenciando este watch (decisão D-j)
	var count int
	err := r.db.QueryRow(
		"SELECT COUNT(*) FROM rule_watches WHERE watch_id = ?", id,
	).Scan(&count)
	if err != nil {
		return fmt.Errorf("checar regras dependentes: %w", err)
	}
	if count > 0 {
		return domain.ErrWatchInUse
	}

	res, err := r.db.Exec("DELETE FROM watches WHERE id = ?", id)
	if err != nil {
		return fmt.Errorf("delete watch: %w", err)
	}
	rows, _ := res.RowsAffected()
	if rows == 0 {
		return domain.ErrNotFound
	}
	return nil
}

// --- helpers internos ---

type rowScanner interface {
	Scan(dest ...any) error
}

func scanWatch(row *sql.Row) (*domain.Watch, error) {
	w, err := doScanWatch(row)
	if errors.Is(err, sql.ErrNoRows) {
		return nil, domain.ErrNotFound
	}
	return w, err
}

func scanWatchRows(row rowScanner) (*domain.Watch, error) {
	return doScanWatch(row)
}

func doScanWatch(row rowScanner) (*domain.Watch, error) {
	var w domain.Watch
	var active, recursive int
	err := row.Scan(&w.ID, &w.Name, &w.Path, &active, &recursive, &w.CreatedAt, &w.UpdatedAt)
	if err != nil {
		return nil, err
	}
	w.Active = active != 0
	w.Recursive = recursive != 0
	return &w, nil
}

func boolToInt(b bool) int {
	if b {
		return 1
	}
	return 0
}

func isUniqueConstraint(err error) bool {
	if err == nil {
		return false
	}
	// modernc.org/sqlite retorna mensagens com "UNIQUE constraint failed"
	return strings.Contains(err.Error(), "UNIQUE constraint failed") ||
		strings.Contains(err.Error(), "constraint failed: UNIQUE")
}
1.7. Implementar WatchRepo testes
internal/storage/watch_repo_test.go:

go
package storage_test

import (
	"errors"
	"testing"

	"juridico-watcher/internal/domain"
	"juridico-watcher/internal/storage"
	"juridico-watcher/test/testhelpers"

	"github.com/stretchr/testify/assert"
	"github.com/stretchr/testify/require"
)

func newWatchRepo(t *testing.T) *storage.WatchRepo {
	t.Helper()
	db := testhelpers.TempDB(t)
	return storage.NewWatchRepo(db)
}

func TestWatchRepo_Create_PersisteEDevolveID(t *testing.T) {
	repo := newWatchRepo(t)
	w := &domain.Watch{Name: "digitalizadoras", Path: "/tmp/x", Active: true, Recursive: true}

	err := repo.Create(w)
	require.NoError(t, err)
	assert.NotZero(t, w.ID)
	assert.False(t, w.CreatedAt.IsZero())
}

func TestWatchRepo_GetByID_RetornaWatchSalvo(t *testing.T) {
	repo := newWatchRepo(t)
	original := &domain.Watch{Name: "a", Path: "/x", Active: true, Recursive: false}
	require.NoError(t, repo.Create(original))

	loaded, err := repo.GetByID(original.ID)
	require.NoError(t, err)
	assert.Equal(t, original.Name, loaded.Name)
	assert.Equal(t, original.Path, loaded.Path)
	assert.Equal(t, original.Active, loaded.Active)
	assert.Equal(t, original.Recursive, loaded.Recursive)
}

func TestWatchRepo_GetByName_RetornaWatchSalvo(t *testing.T) {
	repo := newWatchRepo(t)
	require.NoError(t, repo.Create(&domain.Watch{Name: "digitalizadoras", Path: "/x", Active: true}))

	loaded, err := repo.GetByName("digitalizadoras")
	require.NoError(t, err)
	assert.Equal(t, "digitalizadoras", loaded.Name)
}

func TestWatchRepo_GetByName_NaoExiste_RetornaErrNotFound(t *testing.T) {
	repo := newWatchRepo(t)
	_, err := repo.GetByName("inexistente")
	assert.True(t, errors.Is(err, domain.ErrNotFound))
}

func TestWatchRepo_GetByID_NaoExiste_RetornaErrNotFound(t *testing.T) {
	repo := newWatchRepo(t)
	_, err := repo.GetByID(9999)
	assert.True(t, errors.Is(err, domain.ErrNotFound))
}

func TestWatchRepo_List_RetornaTodosOrdenadosPorNome(t *testing.T) {
	repo := newWatchRepo(t)
	require.NoError(t, repo.Create(&domain.Watch{Name: "zeta", Path: "/z"}))
	require.NoError(t, repo.Create(&domain.Watch{Name: "alfa", Path: "/a"}))
	require.NoError(t, repo.Create(&domain.Watch{Name: "beta", Path: "/b"}))

	list, err := repo.List()
	require.NoError(t, err)
	require.Len(t, list, 3)
	assert.Equal(t, "alfa", list[0].Name)
	assert.Equal(t, "beta", list[1].Name)
	assert.Equal(t, "zeta", list[2].Name)
}

func TestWatchRepo_Update_PersisteAlteracoes(t *testing.T) {
	repo := newWatchRepo(t)
	w := &domain.Watch{Name: "a", Path: "/x", Active: true, Recursive: true}
	require.NoError(t, repo.Create(w))

	w.Name = "b"
	w.Path = "/y"
	w.Active = false
	require.NoError(t, repo.Update(w))

	loaded, err := repo.GetByID(w.ID)
	require.NoError(t, err)
	assert.Equal(t, "b", loaded.Name)
	assert.Equal(t, "/y", loaded.Path)
	assert.False(t, loaded.Active)
}

func TestWatchRepo_Update_NaoExiste_RetornaErrNotFound(t *testing.T) {
	repo := newWatchRepo(t)
	w := &domain.Watch{ID: 9999, Name: "x", Path: "/x"}
	err := repo.Update(w)
	assert.True(t, errors.Is(err, domain.ErrNotFound))
}

func TestWatchRepo_Delete_Remove(t *testing.T) {
	repo := newWatchRepo(t)
	w := &domain.Watch{Name: "a", Path: "/x"}
	require.NoError(t, repo.Create(w))

	require.NoError(t, repo.Delete(w.ID))
	_, err := repo.GetByID(w.ID)
	assert.True(t, errors.Is(err, domain.ErrNotFound))
}

func TestWatchRepo_NomeUnico_CreateDuplicado_Falha(t *testing.T) {
	repo := newWatchRepo(t)
	require.NoError(t, repo.Create(&domain.Watch{Name: "a", Path: "/x"}))

	err := repo.Create(&domain.Watch{Name: "a", Path: "/y"})
	assert.True(t, errors.Is(err, domain.ErrDuplicateName))
}

// O teste TestWatchRepo_Delete_ComRegrasReferenciando_RetornaErro
// será adicionado depois que o RuleRepo existir, na próxima sub-tarefa.
1.8. Implementar RuleRepo
Esse é o repositório mais complexo porque Rule tem Extractions, Conditions, Actions e WatchIDs aninhados. Vamos manter as operações atômicas com transação.

internal/storage/rule_repo.go:

go
package storage

import (
	"database/sql"
	"errors"
	"fmt"
	"time"

	"juridico-watcher/internal/domain"
)

type RuleRepo struct {
	db *sql.DB
}

func NewRuleRepo(db *sql.DB) *RuleRepo {
	return &RuleRepo{db: db}
}

func (r *RuleRepo) Create(rule *domain.Rule) error {
	now := time.Now().UTC()
	tx, err := r.db.Begin()
	if err != nil {
		return fmt.Errorf("begin tx: %w", err)
	}
	defer tx.Rollback()

	res, err := tx.Exec(
		`INSERT INTO rules (name, priority, active, created_at, updated_at)
		 VALUES (?, ?, ?, ?, ?)`,
		rule.Name, rule.Priority, boolToInt(rule.Active), now, now,
	)
	if err != nil {
		if isUniqueConstraint(err) {
			return fmt.Errorf("%w: %s", domain.ErrDuplicateName, rule.Name)
		}
		return fmt.Errorf("insert rule: %w", err)
	}
	rule.ID, _ = res.LastInsertId()

	if err := insertRuleChildren(tx, rule); err != nil {
		return err
	}

	if err := tx.Commit(); err != nil {
		return fmt.Errorf("commit: %w", err)
	}
	rule.CreatedAt = now
	rule.UpdatedAt = now
	return nil
}

func (r *RuleRepo) GetByID(id int64) (*domain.Rule, error) {
	row := r.db.QueryRow(
		`SELECT id, name, priority, active, created_at, updated_at
		 FROM rules WHERE id = ?`, id,
	)
	rule, err := scanRule(row)
	if err != nil {
		return nil, err
	}
	if err := r.loadChildren(rule); err != nil {
		return nil, err
	}
	return rule, nil
}

func (r *RuleRepo) List() ([]*domain.Rule, error) {
	rows, err := r.db.Query(
		`SELECT id, name, priority, active, created_at, updated_at
		 FROM rules ORDER BY priority ASC, name ASC`,
	)
	if err != nil {
		return nil, fmt.Errorf("list rules: %w", err)
	}
	defer rows.Close()

	var out []*domain.Rule
	for rows.Next() {
		rule, err := doScanRule(rows)
		if err != nil {
			return nil, err
		}
		out = append(out, rule)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}

	for _, rule := range out {
		if err := r.loadChildren(rule); err != nil {
			return nil, err
		}
	}
	return out, nil
}

// ListByWatchID retorna todas as regras ativas que se aplicam a um watch específico,
// ordenadas por prioridade. Usado pelo motor de regras (Sprint 4).
func (r *RuleRepo) ListByWatchID(watchID int64) ([]*domain.Rule, error) {
	rows, err := r.db.Query(`
		SELECT r.id, r.name, r.priority, r.active, r.created_at, r.updated_at
		FROM rules r
		INNER JOIN rule_watches rw ON rw.rule_id = r.id
		WHERE rw.watch_id = ? AND r.active = 1
		ORDER BY r.priority ASC, r.name ASC
	`, watchID)
	if err != nil {
		return nil, err
	}
	defer rows.Close()

	var out []*domain.Rule
	for rows.Next() {
		rule, err := doScanRule(rows)
		if err != nil {
			return nil, err
		}
		out = append(out, rule)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	for _, rule := range out {
		if err := r.loadChildren(rule); err != nil {
			return nil, err
		}
	}
	return out, nil
}

func (r *RuleRepo) Update(rule *domain.Rule) error {
	now := time.Now().UTC()
	tx, err := r.db.Begin()
	if err != nil {
		return err
	}
	defer tx.Rollback()

	res, err := tx.Exec(
		`UPDATE rules SET name = ?, priority = ?, active = ?, updated_at = ?
		 WHERE id = ?`,
		rule.Name, rule.Priority, boolToInt(rule.Active), now, rule.ID,
	)
	if err != nil {
		if isUniqueConstraint(err) {
			return fmt.Errorf("%w: %s", domain.ErrDuplicateName, rule.Name)
		}
		return err
	}
	rows, _ := res.RowsAffected()
	if rows == 0 {
		return domain.ErrNotFound
	}

	// Estratégia simples: deleta filhos antigos e reinsere.
	// Como Rule não é gigante, isso é OK e mantém o código simples.
	for _, table := range []string{"extractions", "conditions", "actions", "rule_watches"} {
		if _, err := tx.Exec(fmt.Sprintf("DELETE FROM %s WHERE rule_id = ?", table), rule.ID); err != nil {
			return fmt.Errorf("limpar %s: %w", table, err)
		}
	}

	if err := insertRuleChildren(tx, rule); err != nil {
		return err
	}

	if err := tx.Commit(); err != nil {
		return err
	}
	rule.UpdatedAt = now
	return nil
}

func (r *RuleRepo) Delete(id int64) error {
	res, err := r.db.Exec("DELETE FROM rules WHERE id = ?", id)
	if err != nil {
		return err
	}
	rows, _ := res.RowsAffected()
	if rows == 0 {
		return domain.ErrNotFound
	}
	return nil
}

// --- internos ---

func insertRuleChildren(tx *sql.Tx, rule *domain.Rule) error {
	for _, watchID := range rule.WatchIDs {
		if _, err := tx.Exec(
			`INSERT INTO rule_watches (rule_id, watch_id) VALUES (?, ?)`,
			rule.ID, watchID,
		); err != nil {
			return fmt.Errorf("inserir rule_watches: %w", err)
		}
	}

	for i := range rule.Extractions {
		ex := &rule.Extractions[i]
		ex.RuleID = rule.ID
		ex.Order = i
		res, err := tx.Exec(
			`INSERT INTO extractions (rule_id, variable_name, start_delim, end_delim, sort_order)
			 VALUES (?, ?, ?, ?, ?)`,
			ex.RuleID, ex.VariableName, ex.StartDelim, ex.EndDelim, ex.Order,
		)
		if err != nil {
			return fmt.Errorf("inserir extraction: %w", err)
		}
		ex.ID, _ = res.LastInsertId()
	}

	for i := range rule.Conditions {
		c := &rule.Conditions[i]
		c.RuleID = rule.ID
		c.Order = i
		res, err := tx.Exec(
			`INSERT INTO conditions (rule_id, variable_name, operator, value, sort_order)
			 VALUES (?, ?, ?, ?, ?)`,
			c.RuleID, c.VariableName, string(c.Operator), c.Value, c.Order,
		)
		if err != nil {
			return fmt.Errorf("inserir condition: %w", err)
		}
		c.ID, _ = res.LastInsertId()
	}

	for i := range rule.Actions {
		a := &rule.Actions[i]
		a.RuleID = rule.ID
		a.Order = i
		res, err := tx.Exec(
			`INSERT INTO actions (rule_id, type, target, sort_order)
			 VALUES (?, ?, ?, ?)`,
			a.RuleID, string(a.Type), a.Target, a.Order,
		)
		if err != nil {
			return fmt.Errorf("inserir action: %w", err)
		}
		a.ID, _ = res.LastInsertId()
	}

	return nil
}

func (r *RuleRepo) loadChildren(rule *domain.Rule) error {
	// Extractions
	rows, err := r.db.Query(
		`SELECT id, rule_id, variable_name, start_delim, end_delim, sort_order
		 FROM extractions WHERE rule_id = ? ORDER BY sort_order ASC`, rule.ID,
	)
	if err != nil {
		return err
	}
	for rows.Next() {
		var ex domain.Extraction
		if err := rows.Scan(&ex.ID, &ex.RuleID, &ex.VariableName, &ex.StartDelim, &ex.EndDelim, &ex.Order); err != nil {
			rows.Close()
			return err
		}
		rule.Extractions = append(rule.Extractions, ex)
	}
	rows.Close()

	// Conditions
	rows, err = r.db.Query(
		`SELECT id, rule_id, variable_name, operator, value, sort_order
		 FROM conditions WHERE rule_id = ? ORDER BY sort_order ASC`, rule.ID,
	)
	if err != nil {
		return err
	}
	for rows.Next() {
		var c domain.Condition
		var op string
		if err := rows.Scan(&c.ID, &c.RuleID, &c.VariableName, &op, &c.Value, &c.Order); err != nil {
			rows.Close()
			return err
		}
		c.Operator = domain.Operator(op)
		rule.Conditions = append(rule.Conditions, c)
	}
	rows.Close()

	// Actions
	rows, err = r.db.Query(
		`SELECT id, rule_id, type, target, sort_order
		 FROM actions WHERE rule_id = ? ORDER BY sort_order ASC`, rule.ID,
	)
	if err != nil {
		return err
	}
	for rows.Next() {
		var a domain.Action
		var typ string
		if err := rows.Scan(&a.ID, &a.RuleID, &typ, &a.Target, &a.Order); err != nil {
			rows.Close()
			return err
		}
		a.Type = domain.ActionType(typ)
		rule.Actions = append(rule.Actions, a)
	}
	rows.Close()

	// WatchIDs
	rows, err = r.db.Query(
		`SELECT watch_id FROM rule_watches WHERE rule_id = ?`, rule.ID,
	)
	if err != nil {
		return err
	}
	defer rows.Close()
	for rows.Next() {
		var wid int64
		if err := rows.Scan(&wid); err != nil {
			return err
		}
		rule.WatchIDs = append(rule.WatchIDs, wid)
	}
	return rows.Err()
}

func scanRule(row *sql.Row) (*domain.Rule, error) {
	rule, err := doScanRule(row)
	if errors.Is(err, sql.ErrNoRows) {
		return nil, domain.ErrNotFound
	}
	return rule, err
}

func doScanRule(row rowScanner) (*domain.Rule, error) {
	var rule domain.Rule
	var active int
	err := row.Scan(&rule.ID, &rule.Name, &rule.Priority, &active, &rule.CreatedAt, &rule.UpdatedAt)
	if err != nil {
		return nil, err
	}
	rule.Active = active != 0
	return &rule, nil
}
1.9. Testes do RuleRepo
internal/storage/rule_repo_test.go:

go
package storage_test

import (
	"errors"
	"testing"

	"juridico-watcher/internal/domain"
	"juridico-watcher/internal/storage"
	"juridico-watcher/test/testhelpers"

	"github.com/stretchr/testify/assert"
	"github.com/stretchr/testify/require"
)

// helper: cria DB com um watch e retorna repos + watch
func setupRuleTest(t *testing.T) (*storage.RuleRepo, *storage.WatchRepo, *domain.Watch) {
	t.Helper()
	db := testhelpers.TempDB(t)
	wr := storage.NewWatchRepo(db)
	rr := storage.NewRuleRepo(db)

	w := &domain.Watch{Name: "digitalizadoras", Path: "/tmp/x", Active: true, Recursive: true}
	require.NoError(t, wr.Create(w))
	return rr, wr, w
}

func TestRuleRepo_Create_PersisteEDevolveID(t *testing.T) {
	rr, _, w := setupRuleTest(t)
	rule := &domain.Rule{
		Name:     "procuração",
		Priority: 100,
		Active:   true,
		WatchIDs: []int64{w.ID},
		Extractions: []domain.Extraction{
			{VariableName: "nome", StartDelim: "outorgante:", EndDelim: ","},
			{VariableName: "tipo", StartDelim: "", EndDelim: "pelo"},
		},
		Conditions: []domain.Condition{
			{VariableName: "tipo", Operator: domain.OpEquals, Value: "procuração"},
		},
		Actions: []domain.Action{
			{Type: domain.ActionCreateFolder, Target: "{nome}"},
			{Type: domain.ActionMove, Target: "{nome}"},
			{Type: domain.ActionRename, Target: "procuracao_{nome}"},
		},
	}

	err := rr.Create(rule)
	require.NoError(t, err)
	assert.NotZero(t, rule.ID)

	// IDs dos filhos foram preenchidos
	for _, ex := range rule.Extractions {
		assert.NotZero(t, ex.ID)
		assert.Equal(t, rule.ID, ex.RuleID)
	}
}

func TestRuleRepo_GetByID_RetornaRegraComTodosFilhos(t *testing.T) {
	rr, _, w := setupRuleTest(t)
	original := &domain.Rule{
		Name:     "r",
		Active:   true,
		WatchIDs: []int64{w.ID},
		Extractions: []domain.Extraction{
			{VariableName: "v1", StartDelim: "a", EndDelim: "b"},
		},
		Conditions: []domain.Condition{
			{VariableName: "v1", Operator: domain.OpContains, Value: "x"},
		},
		Actions: []domain.Action{
			{Type: domain.ActionCreateFolder, Target: "{v1}"},
		},
	}
	require.NoError(t, rr.Create(original))

	loaded, err := rr.GetByID(original.ID)
	require.NoError(t, err)
	assert.Equal(t, "r", loaded.Name)
	require.Len(t, loaded.Extractions, 1)
	assert.Equal(t, "v1", loaded.Extractions[0].VariableName)
	require.Len(t, loaded.Conditions, 1)
	assert.Equal(t, domain.OpContains, loaded.Conditions[0].Operator)
	require.Len(t, loaded.Actions, 1)
	assert.Equal(t, domain.ActionCreateFolder, loaded.Actions[0].Type)
	require.Len(t, loaded.WatchIDs, 1)
	assert.Equal(t, w.ID, loaded.WatchIDs[0])
}

func TestRuleRepo_List_RetornaTodasOrdenadasPorPrioridade(t *testing.T) {
	rr, _, w := setupRuleTest(t)
	require.NoError(t, rr.Create(&domain.Rule{Name: "c", Priority: 30, WatchIDs: []int64{w.ID}}))
	require.NoError(t, rr.Create(&domain.Rule{Name: "a", Priority: 10, WatchIDs: []int64{w.ID}}))
	require.NoError(t, rr.Create(&domain.Rule{Name: "b", Priority: 20, WatchIDs: []int64{w.ID}}))

	list, err := rr.List()
	require.NoError(t, err)
	require.Len(t, list, 3)
	assert.Equal(t, "a", list[0].Name)
	assert.Equal(t, "b", list[1].Name)
	assert.Equal(t, "c", list[2].Name)
}

func TestRuleRepo_ListByWatchID_FiltraCorretamente(t *testing.T) {
	rr, wr, w1 := setupRuleTest(t)
	w2 := &domain.Watch{Name: "outra", Path: "/tmp/y", Active: true}
	require.NoError(t, wr.Create(w2))

	require.NoError(t, rr.Create(&domain.Rule{Name: "r1", Active: true, WatchIDs: []int64{w1.ID}}))
	require.NoError(t, rr.Create(&domain.Rule{Name: "r2", Active: true, WatchIDs: []int64{w2.ID}}))
	require.NoError(t, rr.Create(&domain.Rule{Name: "r3", Active: true, WatchIDs: []int64{w1.ID, w2.ID}}))
	require.NoError(t, rr.Create(&domain.Rule{Name: "r4_inactive", Active: false, WatchIDs: []int64{w1.ID}}))

	list, err := rr.ListByWatchID(w1.ID)
	require.NoError(t, err)
	// r1 e r3 (não inclui r2 nem r4 inativa)
	require.Len(t, list, 2)
	names := []string{list[0].Name, list[1].Name}
	assert.Contains(t, names, "r1")
	assert.Contains(t, names, "r3")
}

func TestRuleRepo_Update_SubstituiFilhos(t *testing.T) {
	rr, _, w := setupRuleTest(t)
	rule := &domain.Rule{
		Name:     "r",
		Active:   true,
		WatchIDs: []int64{w.ID},
		Extractions: []domain.Extraction{
			{VariableName: "v1", StartDelim: "a", EndDelim: "b"},
		},
	}
	require.NoError(t, rr.Create(rule))

	rule.Extractions = []domain.Extraction{
		{VariableName: "v2", StartDelim: "x", EndDelim: "y"},
		{VariableName: "v3", StartDelim: "p", EndDelim: "q"},
	}
	require.NoError(t, rr.Update(rule))

	loaded, err := rr.GetByID(rule.ID)
	require.NoError(t, err)
	require.Len(t, loaded.Extractions, 2)
	assert.Equal(t, "v2", loaded.Extractions[0].VariableName)
	assert.Equal(t, "v3", loaded.Extractions[1].VariableName)
}

func TestRuleRepo_Delete_Remove(t *testing.T) {
	rr, _, w := setupRuleTest(t)
	rule := &domain.Rule{Name: "r", Active: true, WatchIDs: []int64{w.ID}}
	require.NoError(t, rr.Create(rule))

	require.NoError(t, rr.Delete(rule.ID))
	_, err := rr.GetByID(rule.ID)
	assert.True(t, errors.Is(err, domain.ErrNotFound))
}

func TestRuleRepo_NomeUnico_CreateDuplicado_Falha(t *testing.T) {
	rr, _, w := setupRuleTest(t)
	require.NoError(t, rr.Create(&domain.Rule{Name: "r", WatchIDs: []int64{w.ID}}))

	err := rr.Create(&domain.Rule{Name: "r", WatchIDs: []int64{w.ID}})
	assert.True(t, errors.Is(err, domain.ErrDuplicateName))
}

// Agora o teste pendente da Sprint 1.7:
func TestWatchRepo_Delete_ComRegrasReferenciando_RetornaErro(t *testing.T) {
	rr, wr, w := setupRuleTest(t)
	require.NoError(t, rr.Create(&domain.Rule{Name: "r", WatchIDs: []int64{w.ID}}))

	err := wr.Delete(w.ID)
	assert.True(t, errors.Is(err, domain.ErrWatchInUse))
}
1.10. Implementar ProcessedRepo
internal/storage/processed_repo.go:

go
package storage

import (
	"database/sql"
	"errors"
	"fmt"
	"strings"
	"time"

	"juridico-watcher/internal/domain"
)

type ProcessedRepo struct {
	db *sql.DB
}

func NewProcessedRepo(db *sql.DB) *ProcessedRepo {
	return &ProcessedRepo{db: db}
}

// Record cria um registro de processamento. Se já existir (mesmo hash + rule),
// retorna nil silenciosamente — o motor de regras consulta antes de chamar Record,
// mas a UNIQUE constraint serve como segurança extra.
func (r *ProcessedRepo) Record(p *domain.ProcessedDoc) error {
	now := time.Now().UTC()
	res, err := r.db.Exec(
		`INSERT INTO processed_documents (file_hash, original_path, rule_id, status, error_msg, processed_at)
		 VALUES (?, ?, ?, ?, ?, ?)`,
		p.FileHash, p.OriginalPath, p.RuleID, string(p.Status), p.ErrorMsg, now,
	)
	if err != nil {
		// Race condition: outro processo registrou primeiro. OK.
		if strings.Contains(err.Error(), "UNIQUE constraint failed") {
			return nil
		}
		return fmt.Errorf("record processed: %w", err)
	}
	p.ID, _ = res.LastInsertId()
	p.ProcessedAt = now
	return nil
}

// HasBeenProcessed retorna true se este hash já foi processado por esta regra.
func (r *ProcessedRepo) HasBeenProcessed(fileHash string, ruleID int64) (bool, error) {
	var exists int
	err := r.db.QueryRow(
		`SELECT COUNT(*) FROM processed_documents WHERE file_hash = ? AND rule_id = ?`,
		fileHash, ruleID,
	).Scan(&exists)
	if err != nil {
		return false, err
	}
	return exists > 0, nil
}

// GetByHash retorna todos os processamentos de um arquivo (todas as regras).
func (r *ProcessedRepo) GetByHash(fileHash string) ([]*domain.ProcessedDoc, error) {
	rows, err := r.db.Query(
		`SELECT id, file_hash, original_path, rule_id, status, error_msg, processed_at
		 FROM processed_documents WHERE file_hash = ? ORDER BY processed_at DESC`,
		fileHash,
	)
	if err != nil {
		return nil, err
	}
	defer rows.Close()

	var out []*domain.ProcessedDoc
	for rows.Next() {
		var p domain.ProcessedDoc
		var status string
		if err := rows.Scan(&p.ID, &p.FileHash, &p.OriginalPath, &p.RuleID, &status, &p.ErrorMsg, &p.ProcessedAt); err != nil {
			return nil, err
		}
		p.Status = domain.ProcessingStatus(status)
		out = append(out, &p)
	}
	return out, rows.Err()
}

// ListRecent retorna os N processamentos mais recentes (para histórico na UI).
func (r *ProcessedRepo) ListRecent(limit int) ([]*domain.ProcessedDoc, error) {
	rows, err := r.db.Query(
		`SELECT id, file_hash, original_path, rule_id, status, error_msg, processed_at
		 FROM processed_documents ORDER BY processed_at DESC LIMIT ?`,
		limit,
	)
	if err != nil {
		return nil, err
	}
	defer rows.Close()

	var out []*domain.ProcessedDoc
	for rows.Next() {
		var p domain.ProcessedDoc
		var status string
		if err := rows.Scan(&p.ID, &p.FileHash, &p.OriginalPath, &p.RuleID, &status, &p.ErrorMsg, &p.ProcessedAt); err != nil {
			return nil, err
		}
		p.Status = domain.ProcessingStatus(status)
		out = append(out, &p)
	}
	return out, rows.Err()
}

// suppress unused warning
var _ = errors.New
var _ = sql.ErrNoRows
1.11. Testes do ProcessedRepo
internal/storage/processed_repo_test.go:

go
package storage_test

import (
	"testing"

	"juridico-watcher/internal/domain"
	"juridico-watcher/internal/storage"
	"juridico-watcher/test/testhelpers"

	"github.com/stretchr/testify/assert"
	"github.com/stretchr/testify/require"
)

func setupProcessedTest(t *testing.T) (*storage.ProcessedRepo, *domain.Rule) {
	t.Helper()
	db := testhelpers.TempDB(t)
	wr := storage.NewWatchRepo(db)
	rr := storage.NewRuleRepo(db)
	pr := storage.NewProcessedRepo(db)

	w := &domain.Watch{Name: "w", Path: "/x", Active: true}
	require.NoError(t, wr.Create(w))
	rule := &domain.Rule{Name: "r", Active: true, WatchIDs: []int64{w.ID}}
	require.NoError(t, rr.Create(rule))
	return pr, rule
}

func TestProcessedRepo_Record_Persiste(t *testing.T) {
	pr, rule := setupProcessedTest(t)
	p := &domain.ProcessedDoc{
		FileHash:     "abc123",
		OriginalPath: "/x/file.pdf",
		RuleID:       rule.ID,
		Status:       domain.StatusSuccess,
	}
	require.NoError(t, pr.Record(p))
	assert.NotZero(t, p.ID)
}

func TestProcessedRepo_HasBeenProcessed_True(t *testing.T) {
	pr, rule := setupProcessedTest(t)
	require.NoError(t, pr.Record(&domain.ProcessedDoc{
		FileHash: "abc", OriginalPath: "/x", RuleID: rule.ID, Status: domain.StatusSuccess,
	}))

	processed, err := pr.HasBeenProcessed("abc", rule.ID)
	require.NoError(t, err)
	assert.True(t, processed)
}

func TestProcessedRepo_HasBeenProcessed_False(t *testing.T) {
	pr, rule := setupProcessedTest(t)
	processed, err := pr.HasBeenProcessed("nunca-visto", rule.ID)
	require.NoError(t, err)
	assert.False(t, processed)
}

func TestProcessedRepo_RecordDuplicado_NaoFalha(t *testing.T) {
	pr, rule := setupProcessedTest(t)
	p := &domain.ProcessedDoc{FileHash: "h", OriginalPath: "/x", RuleID: rule.ID, Status: domain.StatusSuccess}
	require.NoError(t, pr.Record(p))
	// Segundo Record com mesmo (hash, rule) — não deve falhar (UNIQUE absorvido)
	p2 := &domain.ProcessedDoc{FileHash: "h", OriginalPath: "/x", RuleID: rule.ID, Status: domain.StatusSuccess}
	assert.NoError(t, pr.Record(p2))
}

func TestProcessedRepo_GetByHash_RetornaTodasRegras(t *testing.T) {
	pr, rule := setupProcessedTest(t)
	require.NoError(t, pr.Record(&domain.ProcessedDoc{FileHash: "h", OriginalPath: "/x", RuleID: rule.ID, Status: domain.StatusSuccess}))

	list, err := pr.GetByHash("h")
	require.NoError(t, err)
	require.Len(t, list, 1)
	assert.Equal(t, domain.StatusSuccess, list[0].Status)
}

func TestProcessedRepo_ListRecent_OrdenadosPorData(t *testing.T) {
	pr, rule := setupProcessedTest(t)
	require.NoError(t, pr.Record(&domain.ProcessedDoc{FileHash: "h1", OriginalPath: "/a", RuleID: rule.ID, Status: domain.StatusSuccess}))
	require.NoError(t, pr.Record(&domain.ProcessedDoc{FileHash: "h2", OriginalPath: "/b", RuleID: rule.ID, Status: domain.StatusFailed}))

	list, err := pr.ListRecent(10)
	require.NoError(t, err)
	require.Len(t, list, 2)
	// O último inserido deve vir primeiro
	assert.Equal(t, "h2", list[0].FileHash)
}
1.12. Rodar todos os testes
bash
make test-unit
Você deve ver todos os testes do pacote storage passando. Se algum falhar, ajustamos antes de seguir.

Definition of Done — Sprint 1
 internal/domain/ tem watch.go, rule.go, document.go, errors.go.
 internal/storage/migrations/001_initial.sql aplica sem erro.
 storage.Open(path) cria banco, aplica migrations.
 WatchRepo, RuleRepo, ProcessedRepo implementados com Create/Get/List/Update/Delete onde aplicável.
 ListByWatchID no RuleRepo funciona (necessário para o motor).
 Decisão D-j enforçada via ON DELETE RESTRICT + check explícito no WatchRepo.Delete.
 make test-unit passa 100% para o pacote storage e domínio (domínio não tem testes mas compila).
 Commit feito.
Como me reportar o progresso
Quando terminar a Sprint 1, me avisa de uma destas formas:

Tudo passou: "Sprint 1 ok, pode mandar a 2 e 3."
Tem teste falhando: cola o output do make test-unit (a parte do erro), eu te ajudo a corrigir antes de seguir.
Tem dúvida em algum trecho: aponta o arquivo e a linha, esclareço.
Importante: não pule pra implementar o motor de regras antes de a Sprint 1 estar verde. Se o storage tiver bug, a gente debuga em camada errada depois e perde tempo. Camada por camada.

Bom trabalho. Aguardo seu retorno.

Você disse: PS C:\Users\pc\Documents\juridicwatcher> make test-unit
PS C:\Users\pc\Documents\juridicwatcher> make test-unit
go test -race -count=1 ./internal/...
# github.com/F3rreir4L19/juridico-watcher/internal/engine
internal\engine\actions.go:1:1: expected 'package', found 'EOF'
FAIL    github.com/F3rreir4L19/juridico-watcher/internal/engine [setup failed]
# github.com/F3rreir4L19/juridico-watcher/internal/pdf
internal\pdf\extractor.go:1:1: expected 'package', found 'EOF'
FAIL    github.com/F3rreir4L19/juridico-watcher/internal/pdf [setup failed]
# github.com/F3rreir4L19/juridico-watcher/internal/service
internal\service\rule_service.go:1:1: expected 'package', found 'EOF'
FAIL    github.com/F3rreir4L19/juridico-watcher/internal/service [setup failed]
# github.com/F3rreir4L19/juridico-watcher/internal/storage
internal\storage\processed_repo.go:10:2: package juridico-watcher/internal/domain is not in std (C:\Program Files\Go\src\juridico-watcher\internal\domain)
# github.com/F3rreir4L19/juridico-watcher/internal/storage
internal\storage\sqlite.go:7:2: no required module provides package modernc.org/sqlite; to add it:
        go get modernc.org/sqlite
FAIL    github.com/F3rreir4L19/juridico-watcher/internal/storage [setup failed]
# github.com/F3rreir4L19/juridico-watcher/internal/ui
internal\ui\app.go:1:1: expected 'package', found 'EOF'
FAIL    github.com/F3rreir4L19/juridico-watcher/internal/ui [setup failed]
# github.com/F3rreir4L19/juridico-watcher/internal/watcher
internal\watcher\scanner.go:1:1: expected 'package', found 'EOF'
FAIL    github.com/F3rreir4L19/juridico-watcher/internal/watcher [setup failed]
# github.com/F3rreir4L19/juridico-watcher/internal/domain
internal\domain\document.go:26:2: syntax error: unexpected keyword package after top level declaration
internal\domain\document.go:28:1: syntax error: imports must appear before other declarations
FAIL    github.com/F3rreir4L19/juridico-watcher/internal/domain [build failed]
FAIL
make: *** [test-unit] Error 1
PS C:\Users\pc\Documents\juridicwatcher>

Mostrar mais
09:49
Claude respondeu: Beleza, vamos depurar isso.
Beleza, vamos depurar isso. São três problemas distintos, todos solucionáveis em poucos minutos. Vou explicar cada um e te dar o passo a passo de correção.

Problema 1: Module path errado nos imports
O seu módulo se chama github.com/F3rreir4L19/juridico-watcher (deve estar assim no seu go.mod), mas eu te passei o código com imports usando juridico-watcher/internal/.... Por isso o Go está procurando os pacotes em C:\Program Files\Go\src\juridico-watcher\... (lugar errado).

Confirma rodando:

powershell
type go.mod
Você deve ver na primeira linha algo como:

module github.com/F3rreir4L19/juridico-watcher
Solução: substituir juridico-watcher por github.com/F3rreir4L19/juridico-watcher em todos os imports. PowerShell para fazer isso de uma vez:

powershell
Get-ChildItem -Recurse -Include *.go | ForEach-Object {
    (Get-Content $_.FullName) -replace '"juridico-watcher/', '"github.com/F3rreir4L19/juridico-watcher/' | Set-Content $_.FullName
}
Isso varre todos os .go do projeto e substitui "juridico-watcher/ por "github.com/F3rreir4L19/juridico-watcher/ (a aspa antes garante que só pega imports, não strings literais).

Problema 2: Arquivos .go vazios em pacotes que ainda não implementamos
Esses erros aqui:

internal\engine\actions.go:1:1: expected 'package', found 'EOF'
internal\pdf\extractor.go:1:1: expected 'package', found 'EOF'
internal\service\rule_service.go:1:1: expected 'package', found 'EOF'
internal\ui\app.go:1:1: expected 'package', found 'EOF'
internal\watcher\scanner.go:1:1: expected 'package', found 'EOF'
Significam que você criou arquivos vazios para esses pacotes (provavelmente seguiu a estrutura da Sprint 0 e criou .go placeholders). O Go exige que todo arquivo .go tenha pelo menos a declaração de pacote. Arquivo totalmente vazio quebra o build.

Não preciso desses arquivos ainda — vamos implementá-los nas Sprints 2 a 5. Então a solução mais limpa é deletar esses arquivos vazios. Eles serão recriados nas sprints futuras quando tiverem conteúdo de verdade.

powershell
Remove-Item internal\engine\actions.go -ErrorAction SilentlyContinue
Remove-Item internal\pdf\extractor.go -ErrorAction SilentlyContinue
Remove-Item internal\service\rule_service.go -ErrorAction SilentlyContinue
Remove-Item internal\ui\app.go -ErrorAction SilentlyContinue
Remove-Item internal\watcher\scanner.go -ErrorAction SilentlyContinue
Verifica se tem outros arquivos vazios que sobraram:

powershell
Get-ChildItem -Recurse -Filter *.go | Where-Object { $_.Length -eq 0 } | Select-Object FullName
Se aparecer alguma coisa, deleta também — exceto os de domínio (watch.go, rule.go, document.go, errors.go) e storage (sqlite.go, migrations.go, watch_repo.go, rule_repo.go, processed_repo.go), que devem ter conteúdo da Sprint 1.

Mantenha as pastas vazias (internal/engine, internal/pdf, etc.). Pasta vazia o Go ignora; arquivo .go vazio quebra.

Problema 3: modernc.org/sqlite não está no go.mod
internal\storage\sqlite.go:7:2: no required module provides package modernc.org/sqlite
O go get da Sprint 0 não pegou. Roda:

powershell
go get modernc.org/sqlite@latest
go mod tidy
Problema 4 (provável): erro de sintaxe em document.go
internal\domain\document.go:26:2: syntax error: unexpected keyword package after top level declaration
internal\domain\document.go:28:1: syntax error: imports must appear before other declarations
Isso quase sempre significa que o arquivo tem dois package domain colados, ou o import "time" veio depois de uma declaração. Provavelmente quando você colou, alguma coisa duplicou.

Abre o arquivo e vê: deve começar com uma única vez package domain, depois import "time", depois as structs. Se houver duplicação, deleta o que sobrou.

A versão correta deve ser exatamente assim, do topo até a primeira struct:

go
package domain

import "time"

// ProcessingStatus representa o resultado do processamento de um documento por uma regra.
type ProcessingStatus string
Se quiser garantir, sobrescreve o arquivo inteiro com este conteúdo:

internal/domain/document.go (versão completa, copia tudo):

go
package domain

import "time"

// ProcessingStatus representa o resultado do processamento de um documento por uma regra.
type ProcessingStatus string

const (
	StatusSuccess      ProcessingStatus = "success"
	StatusNoMatch      ProcessingStatus = "no_match"      // regra avaliada mas condição não bateu
	StatusFailed       ProcessingStatus = "failed"        // erro ao executar ação
	StatusSkippedMoved ProcessingStatus = "skipped_moved" // arquivo movido por regra anterior
	StatusNoText       ProcessingStatus = "no_text"       // PDF sem texto extraível
)

// ProcessedDoc registra que um arquivo foi avaliado por uma regra.
// Chave lógica: (FileHash, RuleID).
type ProcessedDoc struct {
	ID           int64
	FileHash     string // SHA-256 do conteúdo
	OriginalPath string // caminho onde estava quando foi processado
	RuleID       int64
	Status       ProcessingStatus
	ErrorMsg     string // preenchido quando Status = failed
	ProcessedAt  time.Time
}
Sequência completa pra resolver
Roda nesta ordem no PowerShell, dentro da pasta do projeto:

powershell
# 1. Corrige os imports
Get-ChildItem -Recurse -Include *.go | ForEach-Object {
    (Get-Content $_.FullName) -replace '"juridico-watcher/', '"github.com/F3rreir4L19/juridico-watcher/' | Set-Content $_.FullName
}

# 2. Remove arquivos vazios das sprints futuras
Remove-Item internal\engine\actions.go -ErrorAction SilentlyContinue
Remove-Item internal\pdf\extractor.go -ErrorAction SilentlyContinue
Remove-Item internal\service\rule_service.go -ErrorAction SilentlyContinue
Remove-Item internal\ui\app.go -ErrorAction SilentlyContinue
Remove-Item internal\watcher\scanner.go -ErrorAction SilentlyContinue

# 3. Verifica se sobrou algum vazio
Get-ChildItem -Recurse -Filter *.go | Where-Object { $_.Length -eq 0 } | Select-Object FullName

# 4. Garante deps
go get modernc.org/sqlite@latest
go mod tidy

# 5. Verifica document.go (abre e confere que só tem 1 "package domain")
type internal\domain\document.go

# 6. Roda os testes
make test-unit
Se ainda der erro depois disso
Cola o novo output e a gente continua. Mas com essas 4 correções, esperado é que make test-unit agora rode os testes do storage e passe todos. Os outros pacotes (engine, pdf, etc.) vão aparecer como [no test files], o que é o comportamento correto — eles serão preenchidos nas próximas sprints.

Uma observação preventiva: pra futuras sprints, eu vou continuar te passando código com imports usando juridico-watcher/... por brevidade. Quando você for colar, lembra de trocar pra github.com/F3rreir4L19/juridico-watcher/.... Ou, melhor: roda esse one-liner do PowerShell sempre depois de colar muito código novo:

powershell
Get-ChildItem -Recurse -Include *.go | ForEach-Object {
    (Get-Content $_.FullName) -replace '"juridico-watcher/', '"github.com/F3rreir4L19/juridico-watcher/' | Set-Content $_.FullName
}
Vira reflexo. Me reporta o resultado.





Claude é uma IA e pode cometer erros. Por favor, verifique as respostas.
